{
  "_comment": "The resolved-input envelope. Every trial removes one refusal from a document that arrives from a producer this builder does not control, so a survivor is a field nobody checks rather than a metric nobody measures.",
  "file": "tools/xtask/src/vfs_products/resolved_inputs.rs",
  "verify": "scripts/dev-shell.sh cargo test -q -p xtask --target \"$(rustc -vV | sed -n 's/^host: //p')\" resolved_inputs >/dev/null 2>&1",
  "build": "scripts/dev-shell.sh cargo build -q -p xtask --target \"$(rustc -vV | sed -n 's/^host: //p')\" >/dev/null 2>&1",
  "trials": [
    {
      "name": "a document of any schema or kind is accepted",
      "scope": "pub fn validate_envelope",
      "scope_end": "\n}\n",
      "find": "    if document.schema != 1 || document.kind != \"kandelo-resolved-vfs-product-inputs\" {",
      "replace": "    if false {"
    },
    {
      "name": "the manifest path is never validated",
      "scope": "pub fn validate_envelope",
      "scope_end": "\n}\n",
      "find": "    validate_repo_path_shape(&document.product.manifest_path)?;",
      "replace": "    let _ = &document.product.manifest_path;"
    },
    {
      "name": "the product output may be a path",
      "scope": "fn validate_output_name",
      "scope_end": "\n}\n",
      "find": "        || value.contains(['/', '\\\\', '\\0'])",
      "replace": "        || false"
    },
    {
      "name": "the product output need not be a VFS image",
      "scope": "fn validate_output_name",
      "scope_end": "\n}\n",
      "find": "        || !(value.ends_with(\".vfs\") || value.ends_with(\".vfs.zst\"))",
      "replace": "        || false"
    },
    {
      "name": "an ABI version of zero is accepted",
      "scope": "pub fn validate_envelope",
      "scope_end": "\n}\n",
      "find": "    if document.target_abi.version == 0 {",
      "replace": "    if false {"
    },
    {
      "name": "the source repository may be anything",
      "scope": "fn validate_repository_identity",
      "scope_end": "\n}\n",
      "find": "    if !ok {",
      "replace": "    if false {"
    },
    {
      "name": "a repository identity may carry extra path segments",
      "scope": "fn validate_repository_identity",
      "scope_end": "\n}\n",
      "find": "    let ok = matches!((parts.next(), parts.next(), parts.next()), (Some(o), Some(n), None)",
      "replace": "    let ok = matches!((parts.next(), parts.next(), parts.next()), (Some(o), Some(n), _)"
    },
    {
      "name": "a local-fixture reference needs no permission",
      "scope": "pub fn validate_envelope",
      "scope_end": "\n}\n",
      "find": "        \"local-fixture\" if allow_local_fixture => {}",
      "replace": "        \"local-fixture\" => {}"
    },
    {
      "name": "inputs need not be sorted or unique",
      "scope": "pub fn validate_envelope",
      "scope_end": "\n}\n",
      "find": "        if pair[0].id >= pair[1].id {",
      "replace": "        if false {"
    },
    {
      "name": "inputs are unbounded",
      "scope": "pub fn validate_envelope",
      "scope_end": "\n}\n",
      "find": "    if document.inputs.len() > MAX_INPUTS {",
      "replace": "    if false {"
    },
    {
      "name": "the manifest path is checked for existence rather than shape",
      "scope": "pub fn validate_envelope",
      "scope_end": "\n}\n",
      "find": "    validate_repo_path_shape(&document.product.manifest_path)?;",
      "replace": "    super::canonical_json::validate_repo_path(\n        std::path::Path::new(\"/nonexistent\"),\n        &document.product.manifest_path,\n    )?;"
    },
    {
      "name": "role and materialization are checked separately rather than together",
      "scope": "fn validate_input(",
      "scope_end": "\n}\n",
      "find": "    let valid = matches!(\n        combination,\n        (\"runtime\", \"embedded\", \"embedded\")\n            | (\"runtime\", \"lazy\", \"lazy-reference\")\n            | (\"runtime\", \"lazy\", \"embedded\")\n            | (\"build\", \"build-only\", \"build-only\")\n    );",
      "replace": "    let valid = matches!(combination.0, \"runtime\" | \"build\");"
    },
    {
      "name": "a lazy declaration may arrive any way at all",
      "scope": "fn validate_input(",
      "scope_end": "\n}\n",
      "find": "            | (\"runtime\", \"lazy\", \"lazy-reference\")\n            | (\"runtime\", \"lazy\", \"embedded\")",
      "replace": "            | (\"runtime\", \"lazy\", _)"
    },
    {
      "name": "an input may be built for another architecture",
      "scope": "fn validate_input(",
      "scope_end": "\n}\n",
      "find": "    if input.architecture != product_architecture {",
      "replace": "    if false {"
    },
    {
      "name": "an input may be of any kind",
      "scope": "fn validate_input(",
      "scope_end": "\n}\n",
      "find": "    if !INPUT_KINDS.contains(&input.kind.as_str()) {",
      "replace": "    if false {"
    },
    {
      "name": "an input's digest is never checked",
      "scope": "fn validate_input(",
      "scope_end": "\n}\n",
      "find": "    validate_sha256(&input.sha256)?;",
      "replace": "    let _ = &input.sha256;"
    },
    {
      "name": "a reference need not bind any digest",
      "scope": "fn validate_reference_binds_digest(",
      "scope_end": "\n}\n",
      "find": "    if !reference.contains(&format!(\"sha256:{digest}\"))\n        && !reference.contains(&format!(\"sha256={digest}\"))\n    {",
      "replace": "    if false {"
    },
    {
      "name": "a reference may bind any digest, not this input's",
      "scope": "fn validate_reference_binds_digest(",
      "scope_end": "\n}\n",
      "find": "    if !reference.contains(&format!(\"sha256:{digest}\"))\n        && !reference.contains(&format!(\"sha256={digest}\"))",
      "replace": "    if !reference.contains(\"sha256:\") && !reference.contains(\"sha256=\")"
    },
    {
      "name": "a reference may carry whitespace",
      "scope": "fn validate_reference_binds_digest(",
      "scope_end": "\n}\n",
      "find": "    if reference.chars().any(char::is_whitespace) {",
      "replace": "    if false {"
    },
    {
      "name": "a reference is unbounded in length",
      "scope": "fn validate_reference_binds_digest(",
      "scope_end": "\n}\n",
      "find": "    if reference.len() > 4_096 {",
      "replace": "    if false {"
    },
    {
      "name": "an input's reference is never checked",
      "scope": "fn validate_input(",
      "scope_end": "\n}\n",
      "find": "        validate_reference_binds_digest(reference, &input.sha256, label)?;",
      "replace": "        let _ = reference;"
    },
    {
      "name": "any kind of input may carry a package descriptor",
      "scope": "fn validate_input(",
      "scope_end": "\n}\n",
      "find": "        (Some(_), other) => {\n            return Err(format!(\"{label} descriptor is only valid for package outputs, not {other}\"))\n        }",
      "replace": "        (Some(_), _other) => {}"
    },
    {
      "name": "a descriptor's reference binds the input's digest rather than its own",
      "scope": "fn validate_input(",
      "scope_end": "\n}\n",
      "find": "        &descriptor.reference,\n        &descriptor.sha256,",
      "replace": "        &descriptor.reference,\n        &input.sha256,"
    },
    {
      "name": "a descriptor's path is never shape-checked",
      "scope": "fn validate_input(",
      "scope_end": "\n}\n",
      "find": "    validate_repo_path_shape(&descriptor.path)\n        .map_err(|e| format!(\"{label} descriptor path: {e}\"))?;",
      "replace": "    let _ = &descriptor.path;"
    },
    {
      "name": "an input's path is never shape-checked",
      "scope": "fn validate_input(",
      "scope_end": "\n}\n",
      "find": "        validate_repo_path_shape(path)\n            .map_err(|e| format!(\"{label} path: {e}\"))?;",
      "replace": "        let _ = path;"
    },
    {
      "name": "a descriptor's digest is never checked",
      "scope": "fn validate_input(",
      "scope_end": "\n}\n",
      "find": "    validate_sha256(&descriptor.sha256)\n        .map_err(|e| format!(\"{label} descriptor: {e}\"))?;",
      "replace": "    let _ = &descriptor.sha256;"
    },
    {
      "name": "a Pages input reference need not bind the input's id",
      "scope": "fn validate_reference_shape(",
      "scope_end": "\n}\n",
      "find": "            && &captures[1] == input.id\n            && captures[1] == captures[3]",
      "replace": "            && captures[1] == captures[3]"
    },
    {
      "name": "a Pages input reference need not bind the byte count",
      "scope": "fn validate_reference_shape(",
      "scope_end": "\n}\n",
      "find": "            && captures[5].parse::<u64>().ok() == Some(input.bytes)",
      "replace": "            && captures[5].parse::<u64>().is_ok()"
    },
    {
      "name": "a Pages product reference need not bind the ABI version",
      "scope": "fn validate_reference_shape(",
      "scope_end": "\n}\n",
      "find": "            && captures[4].parse::<u32>().ok() == Some(target_abi_version)",
      "replace": "            && captures[4].parse::<u32>().is_ok()"
    },
    {
      "name": "a Pages product may be fetched lazily",
      "scope": "fn validate_reference_shape(",
      "scope_end": "\n}\n",
      "find": "        if input.effective_materialization != \"embedded\" {",
      "replace": "        if false {"
    },
    {
      "name": "a Pages reference is accepted outside the canonical class",
      "scope": "fn validate_reference_shape(",
      "scope_end": "\n}\n",
      "find": "        let ok = reference_class == \"canonical\"\n            && &captures[1] == input.id",
      "replace": "        let ok = true\n            && &captures[1] == input.id"
    },
    {
      "name": "a product image may be addressed as an input",
      "scope": "fn validate_reference_shape(",
      "scope_end": "\n}\n",
      "find": "            && input.kind != \"product-image\"\n            && input.effective_materialization == \"lazy-reference\";",
      "replace": "            && input.effective_materialization == \"lazy-reference\";"
    },
    {
      "name": "a reference shape is never checked at all",
      "scope": "fn validate_input(",
      "scope_end": "\n}\n",
      "find": "        validate_reference_shape(reference, input, reference_class, target_abi_version, label)?;",
      "replace": "        let _ = (reference_class, target_abi_version);"
    }
  ]
}
