{
 "file": "host/src/platform/native-metadata.ts",
 "verify": "cd host && npx vitest run test/browser-worker-node-globals.test.ts >/dev/null 2>&1",
 "trials": [
  {
   "name": "the Windows check reads process.platform unguarded, as it shipped",
   "scope": "const SYNTHESIZE_POSIX_MODE",
   "scope_end": ";\n",
   "find": "  typeof process !== \"undefined\" && process.platform === \"win32\";",
   "replace": "  process.platform === \"win32\";"
  },
  {
   "name": "the guard short-circuits with || , which runs the right side exactly when process is undefined",
   "scope": "const SYNTHESIZE_POSIX_MODE",
   "scope_end": ";\n",
   "find": "  typeof process !== \"undefined\" && process.platform === \"win32\";",
   "replace": "  typeof process !== \"undefined\" || process.platform === \"win32\";"
  },
  {
   "name": "the guard tests the wrong global, leaving process unchecked",
   "scope": "const SYNTHESIZE_POSIX_MODE",
   "scope_end": ";\n",
   "find": "  typeof process !== \"undefined\" && process.platform === \"win32\";",
   "replace": "  typeof Buffer !== \"undefined\" && process.platform === \"win32\";"
  }
 ]
}
