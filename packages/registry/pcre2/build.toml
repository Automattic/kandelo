script_path = "packages/registry/pcre2/build-pcre2.sh"
repo_url    = "https://github.com/Automattic/kandelo.git"
commit      = "cf81d0f7c"
revision    = 1

# The fetched `pcre2-src/` tree is gitignored; the resolver hashes
# these tracked inputs on a clean checkout.
inputs = [
  "packages/registry/pcre2/build-pcre2.sh",
]

[binary]
index_url = "https://github.com/Automattic/kandelo/releases/download/binaries-abi-v{abi}/index.toml"
