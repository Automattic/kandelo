script_path = "packages/registry/libxkbregistry/build-libxkbregistry.sh"
repo_url = "https://github.com/Automattic/kandelo.git"
commit = "cf81d0f7c"
revision = 1

# Shares libxkbcommon's hand-curated config.h — a config change there
# must re-key this package too.
inputs = [
  "packages/registry/libxkbregistry/build-libxkbregistry.sh",
  "packages/registry/libxkbcommon/src/config.h",
]
