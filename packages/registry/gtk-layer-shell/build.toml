script_path = "packages/registry/gtk-layer-shell/build-gtk-layer-shell.sh"
repo_url    = "https://github.com/Automattic/kandelo.git"
commit      = "cf81d0f7c"
revision    = 1

# The fetched `gtk-layer-shell-src/` tree is gitignored; the resolver
# hashes these tracked inputs on a clean checkout.
inputs = [
  "packages/registry/gtk-layer-shell/build-gtk-layer-shell.sh",
]

[binary]
index_url = "https://github.com/Automattic/kandelo/releases/download/binaries-abi-v{abi}/index.toml"
