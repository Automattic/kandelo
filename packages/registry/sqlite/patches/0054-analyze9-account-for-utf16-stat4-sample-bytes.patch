--- test/analyze9.test
+++ test/analyze9.test
@@ -810,7 +810,11 @@ do_test 16.1 {
   set nByte2 [lindex [sqlite3_db_status db SCHEMA_USED 0] 1]
   puts -nonewline " (nByte=$nByte nByte2=$nByte2)"
 
-  expr {$nByte2 > $nByte+900 && $nByte2 < $nByte+1100}
+  set minDelta 900
+  set maxDelta 1100
+  if {[permutation]=="utf16"} { set minDelta 1900; set maxDelta 2100 }
+
+  expr {$nByte2 > $nByte+$minDelta && $nByte2 < $nByte+$maxDelta}
 } {1}
 
 #-------------------------------------------------------------------------
