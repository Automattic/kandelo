--- test/external_reader.test
+++ test/external_reader.test
@@ -26,6 +26,12 @@ if {$::tcl_platform(platform)!="unix"} {
   return
 }
 
+if {[permutation]=="no_mutex_try"} {
+  omit_test external_reader.test "Kandelo builds SQLite with ENABLE_SETLK_TIMEOUT; no_mutex_try forces sqlite3_mutex_try() to fail, but ENABLE_SETLK_TIMEOUT WAL shared-memory exclusive locks intentionally use sqlite3_mutex_try() to avoid deadlocks"
+  finish_test
+  return
+}
+
 do_multiclient_test tn {
 
   set bExternal 1
