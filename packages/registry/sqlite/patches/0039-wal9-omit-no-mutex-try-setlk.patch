--- test/wal9.test
+++ test/wal9.test
@@ -19,6 +19,12 @@
 set testdir [file dirname $argv0]
 source $testdir/tester.tcl
 set testprefix wal9
 
+if {[permutation]=="no_mutex_try"} {
+  omit_test wal9.test "Kandelo builds SQLite with ENABLE_SETLK_TIMEOUT; no_mutex_try forces sqlite3_mutex_try() to fail, but ENABLE_SETLK_TIMEOUT WAL shared-memory exclusive locks intentionally use sqlite3_mutex_try() to avoid deadlocks"
+  finish_test
+  return
+}
+
 sqlite3 db2 test.db
 
 do_execsql_test 1.0 {
