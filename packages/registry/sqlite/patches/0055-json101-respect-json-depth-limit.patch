--- src/test_config.c
+++ src/test_config.c
@@ -788,0 +789,4 @@
+#ifndef SQLITE_JSON_MAX_DEPTH
+# define SQLITE_JSON_MAX_DEPTH 1000
+#endif
+
@@ -795,0 +800 @@
+  LINKVAR( JSON_MAX_DEPTH );
--- test/json101.test
+++ test/json101.test
@@ -816 +816 @@
-  /* Shallow enough to be parsed */
+  /* Shallow enough to be parsed with SQLite's default JSON depth limit */
@@ -818 +818 @@
-} {1}
+} [expr {$SQLITE_JSON_MAX_DEPTH>=1000 ? 1 : 0}]
@@ -820 +820 @@
-  /* Too deep by one */
+  /* Too deep by one for SQLite's default JSON depth limit */
@@ -827 +827 @@
-} {1}
+} [expr {$SQLITE_JSON_MAX_DEPTH>=1000 ? 1 : 0}]
