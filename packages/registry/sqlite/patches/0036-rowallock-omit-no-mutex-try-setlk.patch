--- test/rowallock.test
+++ test/rowallock.test
@@ -17,6 +17,12 @@ source $testdir/tester.tcl
 source $testdir/lock_common.tcl
 set testprefix rowallock
 
+if {[permutation]=="no_mutex_try"} {
+  omit_test rowallock.test "Kandelo builds SQLite with ENABLE_SETLK_TIMEOUT; no_mutex_try forces sqlite3_mutex_try() to fail, but ENABLE_SETLK_TIMEOUT WAL shared-memory exclusive locks intentionally use sqlite3_mutex_try() to avoid deadlocks"
+  finish_test
+  return
+}
+
 set mmap_res 1000000
 ifcapable !mmap {
   set mmap_res 0
