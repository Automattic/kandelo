--- test/misc1.test
+++ test/misc1.test
@@ -286,41 +286,47 @@
 do_test misc1-10.0 {
   execsql {SELECT count(*) FROM manycol}
 } {9}
-do_test misc1-10.1 {
-  set ::where {WHERE x0>=0}
-  for {set i 1} {$i<=99} {incr i} {
-    append ::where " AND x$i<>0"
-  }
-  catchsql "SELECT count(*) FROM manycol $::where"
-} {0 9}
-do_test misc1-10.2 {
-  catchsql "SELECT count(*) FROM manycol $::where AND rowid>0"
-} {0 9}
-do_test misc1-10.3 {
-  regsub "x0>=0" $::where "x0=0" ::where
-  catchsql "DELETE FROM manycol $::where"
-} {0 {}}
-do_test misc1-10.4 {
-  execsql {SELECT count(*) FROM manycol}
-} {8}
-do_test misc1-10.5 {
-  catchsql "DELETE FROM manycol $::where AND rowid>0"
-} {0 {}}
-do_test misc1-10.6 {
-  execsql {SELECT x1 FROM manycol WHERE x0=100}
-} {101}
-do_test misc1-10.7 {
-  regsub "x0=0" $::where "x0=100" ::where
-  catchsql "UPDATE manycol SET x1=x1+1 $::where"
-} {0 {}}
-do_test misc1-10.8 {
-  execsql {SELECT x1 FROM manycol WHERE x0=100}
-} {102}
-do_test misc1-10.9 {
-  catchsql "UPDATE manycol SET x1=x1+1 $::where AND rowid>0"
-} {0 {}}
-do_test misc1-10.10 {
-  execsql {SELECT x1 FROM manycol WHERE x0=100}
-} {103}
+if {$SQLITE_MAX_EXPR_DEPTH>0 && $SQLITE_MAX_EXPR_DEPTH<=100} {
+  foreach tn {10.1 10.2 10.3 10.4 10.5 10.6 10.7 10.8 10.9 10.10} {
+    omit_test misc1-$tn "requires 100-term AND expression chains above Kandelo's SQLITE_MAX_EXPR_DEPTH=$SQLITE_MAX_EXPR_DEPTH browser-safe build limit"
+  }
+} else {
+  do_test misc1-10.1 {
+    set ::where {WHERE x0>=0}
+    for {set i 1} {$i<=99} {incr i} {
+      append ::where " AND x$i<>0"
+    }
+    catchsql "SELECT count(*) FROM manycol $::where"
+  } {0 9}
+  do_test misc1-10.2 {
+    catchsql "SELECT count(*) FROM manycol $::where AND rowid>0"
+  } {0 9}
+  do_test misc1-10.3 {
+    regsub "x0>=0" $::where "x0=0" ::where
+    catchsql "DELETE FROM manycol $::where"
+  } {0 {}}
+  do_test misc1-10.4 {
+    execsql {SELECT count(*) FROM manycol}
+  } {8}
+  do_test misc1-10.5 {
+    catchsql "DELETE FROM manycol $::where AND rowid>0"
+  } {0 {}}
+  do_test misc1-10.6 {
+    execsql {SELECT x1 FROM manycol WHERE x0=100}
+  } {101}
+  do_test misc1-10.7 {
+    regsub "x0=0" $::where "x0=100" ::where
+    catchsql "UPDATE manycol SET x1=x1+1 $::where"
+  } {0 {}}
+  do_test misc1-10.8 {
+    execsql {SELECT x1 FROM manycol WHERE x0=100}
+  } {102}
+  do_test misc1-10.9 {
+    catchsql "UPDATE manycol SET x1=x1+1 $::where AND rowid>0"
+  } {0 {}}
+  do_test misc1-10.10 {
+    execsql {SELECT x1 FROM manycol WHERE x0=100}
+  } {103}
+}
 
 # Make sure the initialization works even if a database is opened while
 # another process has the database locked.
