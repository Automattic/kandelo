--- test/walshared.test
+++ test/walshared.test
@@ -18,6 +18,12 @@ source $testdir/tester.tcl
 
 ifcapable !wal||!shared_cache {finish_test ; return }
 
+if {[permutation]=="no_mutex_try"} {
+  omit_test walshared.test "Kandelo builds SQLite with ENABLE_SETLK_TIMEOUT; no_mutex_try forces sqlite3_mutex_try() to fail, but ENABLE_SETLK_TIMEOUT WAL shared-memory exclusive locks intentionally use sqlite3_mutex_try() to avoid deadlocks"
+  finish_test
+  return
+}
+
 db close
 set ::enable_shared_cache [sqlite3_enable_shared_cache 1]
 
