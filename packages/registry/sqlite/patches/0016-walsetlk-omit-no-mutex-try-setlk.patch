--- test/walsetlk.test
+++ test/walsetlk.test
@@ -18,6 +18,12 @@ source $testdir/lock_common.tcl
 set testprefix walsetlk
 
 ifcapable !wal {finish_test ; return }
+
+if {[permutation]=="no_mutex_try"} {
+  omit_test walsetlk.test "Kandelo builds SQLite with ENABLE_SETLK_TIMEOUT; no_mutex_try forces sqlite3_mutex_try() to fail, but ENABLE_SETLK_TIMEOUT WAL shared-memory exclusive locks intentionally use sqlite3_mutex_try() to avoid deadlocks"
+  finish_test
+  return
+}
 db timeout 1000
 
 #-------------------------------------------------------------------------
