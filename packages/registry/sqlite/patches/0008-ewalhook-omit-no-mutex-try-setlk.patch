--- test/e_walhook.test
+++ test/e_walhook.test
@@ -15,6 +15,11 @@ source $testdir/tester.tcl
 source $testdir/wal_common.tcl
 set testprefix e_walhook
 
+if {[permutation]=="no_mutex_try"} {
+  omit_test e_walhook.test "Kandelo builds SQLite with ENABLE_SETLK_TIMEOUT; no_mutex_try forces sqlite3_mutex_try() to fail, but ENABLE_SETLK_TIMEOUT WAL shared-memory exclusive locks intentionally use sqlite3_mutex_try() to avoid deadlocks"
+  finish_test
+  return
+}
 
 # EVIDENCE-OF: R-00752-43975 The sqlite3_wal_hook() function is used to
 # register a callback that is invoked each time data is committed to a
