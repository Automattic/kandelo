diff --git a/src/modules/graphics/wrap_Font.cpp b/src/modules/graphics/wrap_Font.cpp
index 56b0cca..75e3291 100644
--- a/src/modules/graphics/wrap_Font.cpp
+++ b/src/modules/graphics/wrap_Font.cpp
@@ -25,11 +25,41 @@
 // C++
 #include <algorithm>
 
+#ifdef LOVE_KANDELO
+extern "C" bool kandelo_love_uses_legacy_color_range();
+#endif
+
 namespace love
 {
 namespace graphics
 {
 
+static bool usesKandeloLegacyColorRange()
+{
+#ifdef LOVE_KANDELO
+	return kandelo_love_uses_legacy_color_range();
+#else
+	return false;
+#endif
+}
+
+static Colorf normalizeKandeloColor(Colorf c)
+{
+	if (!usesKandeloLegacyColorRange())
+		return c;
+
+	c.r /= 255.0f;
+	c.g /= 255.0f;
+	c.b /= 255.0f;
+	c.a /= 255.0f;
+	return c;
+}
+
+static float defaultKandeloAlpha()
+{
+	return usesKandeloLegacyColorRange() ? 255.0f : 1.0f;
+}
+
 void luax_checkcoloredstring(lua_State *L, int idx, std::vector<Font::ColoredString> &strings)
 {
 	Font::ColoredString coloredstr;
@@ -51,8 +81,9 @@ void luax_checkcoloredstring(lua_State *L, int idx, std::vector<Font::ColoredStr
 				coloredstr.color.r = (float) luaL_checknumber(L, -4);
 				coloredstr.color.g = (float) luaL_checknumber(L, -3);
 				coloredstr.color.b = (float) luaL_checknumber(L, -2);
-				coloredstr.color.a = (float) luaL_optnumber(L, -1, 1.0);
+				coloredstr.color.a = (float) luaL_optnumber(L, -1, defaultKandeloAlpha());
 
+				coloredstr.color = normalizeKandeloColor(coloredstr.color);
 				lua_pop(L, 4);
 			}
 			else
diff --git a/src/modules/graphics/wrap_Graphics.cpp b/src/modules/graphics/wrap_Graphics.cpp
index f5c6a87..0c1977f 100644
--- a/src/modules/graphics/wrap_Graphics.cpp
+++ b/src/modules/graphics/wrap_Graphics.cpp
@@ -1521,10 +1521,20 @@ static Mesh *newStandardMesh(lua_State *L)
 			v.s = (float) luaL_optnumber(L, -6, 0.0);
 			v.t = (float) luaL_optnumber(L, -5, 0.0);
 
-			v.color.r = (unsigned char) (luax_optnumberclamped01(L, -4, 1.0) * 255.0);
-			v.color.g = (unsigned char) (luax_optnumberclamped01(L, -3, 1.0) * 255.0);
-			v.color.b = (unsigned char) (luax_optnumberclamped01(L, -2, 1.0) * 255.0);
-			v.color.a = (unsigned char) (luax_optnumberclamped01(L, -1, 1.0) * 255.0);
+			if (usesLegacyColorRange())
+			{
+				v.color.r = (unsigned char) std::min(std::max((double) luaL_optnumber(L, -4, 255.0), 0.0), 255.0);
+				v.color.g = (unsigned char) std::min(std::max((double) luaL_optnumber(L, -3, 255.0), 0.0), 255.0);
+				v.color.b = (unsigned char) std::min(std::max((double) luaL_optnumber(L, -2, 255.0), 0.0), 255.0);
+				v.color.a = (unsigned char) std::min(std::max((double) luaL_optnumber(L, -1, defaultAlpha()), 0.0), 255.0);
+			}
+			else
+			{
+				v.color.r = (unsigned char) (luax_optnumberclamped01(L, -4, 1.0) * 255.0);
+				v.color.g = (unsigned char) (luax_optnumberclamped01(L, -3, 1.0) * 255.0);
+				v.color.b = (unsigned char) (luax_optnumberclamped01(L, -2, 1.0) * 255.0);
+				v.color.a = (unsigned char) (luax_optnumberclamped01(L, -1, 1.0) * 255.0);
+			}
 
 			lua_pop(L, 9);
 			vertices.push_back(v);
@@ -2606,10 +2606,16 @@ int w_points(lua_State *L)
 				positions[i].x = luax_checkfloat(L, -6);
 				positions[i].y = luax_checkfloat(L, -5);
 
-				colors[i].r = (float) luax_optnumberclamped01(L, -4, 1.0);
-				colors[i].g = (float) luax_optnumberclamped01(L, -3, 1.0);
-				colors[i].b = (float) luax_optnumberclamped01(L, -2, 1.0);
-				colors[i].a = (float) luax_optnumberclamped01(L, -1, 1.0);
+				Colorf c;
+				c.r = (float) luaL_optnumber(L, -4, usesLegacyColorRange() ? 255.0 : 1.0);
+				c.g = (float) luaL_optnumber(L, -3, usesLegacyColorRange() ? 255.0 : 1.0);
+				c.b = (float) luaL_optnumber(L, -2, usesLegacyColorRange() ? 255.0 : 1.0);
+				c.a = (float) luaL_optnumber(L, -1, defaultAlpha());
+				c = normalizeColor(c);
+				colors[i].r = std::min(std::max(c.r, 0.0f), 1.0f);
+				colors[i].g = std::min(std::max(c.g, 0.0f), 1.0f);
+				colors[i].b = std::min(std::max(c.b, 0.0f), 1.0f);
+				colors[i].a = std::min(std::max(c.a, 0.0f), 1.0f);
 
 				lua_pop(L, 7);
 			}
diff --git a/src/modules/graphics/wrap_ParticleSystem.cpp b/src/modules/graphics/wrap_ParticleSystem.cpp
index c2f6e39..dd0da38 100644
--- a/src/modules/graphics/wrap_ParticleSystem.cpp
+++ b/src/modules/graphics/wrap_ParticleSystem.cpp
@@ -29,11 +29,48 @@
 // C
 #include <cstring>
 
+#ifdef LOVE_KANDELO
+extern "C" bool kandelo_love_uses_legacy_color_range();
+#endif
+
 namespace love
 {
 namespace graphics
 {
 
+static bool usesKandeloLegacyColorRange()
+{
+#ifdef LOVE_KANDELO
+	return kandelo_love_uses_legacy_color_range();
+#else
+	return false;
+#endif
+}
+
+static Colorf normalizeKandeloColor(Colorf c)
+{
+	if (!usesKandeloLegacyColorRange())
+		return c;
+
+	c.r /= 255.0f;
+	c.g /= 255.0f;
+	c.b /= 255.0f;
+	c.a /= 255.0f;
+	return c;
+}
+
+static Colorf denormalizeKandeloColor(Colorf c)
+{
+	if (usesKandeloLegacyColorRange())
+		c *= 255.0f;
+	return c;
+}
+
+static float defaultKandeloAlpha()
+{
+	return usesKandeloLegacyColorRange() ? 255.0f : 1.0f;
+}
+
 ParticleSystem *luax_checkparticlesystem(lua_State *L, int idx)
 {
 	return luax_checktype<ParticleSystem>(L, idx);
@@ -523,8 +560,9 @@ int w_ParticleSystem_setColors(lua_State *L)
 			colors[i].r = (float) luaL_checknumber(L, -4);
 			colors[i].g = (float) luaL_checknumber(L, -3);
 			colors[i].b = (float) luaL_checknumber(L, -2);
-			colors[i].a = (float) luaL_optnumber(L, -1, 1.0);
+			colors[i].a = (float) luaL_optnumber(L, -1, defaultKandeloAlpha());
 
+			colors[i] = normalizeKandeloColor(colors[i]);
 			// pop the color components from the stack
 			lua_pop(L, 4);
 		}
@@ -549,7 +587,8 @@ int w_ParticleSystem_setColors(lua_State *L)
 			colors[i].r = (float) luaL_checknumber(L, 1 + i*4 + 1);
 			colors[i].g = (float) luaL_checknumber(L, 1 + i*4 + 2);
 			colors[i].b = (float) luaL_checknumber(L, 1 + i*4 + 3);
-			colors[i].a = (float) luaL_checknumber(L, 1 + i*4 + 4);
+			colors[i].a = (float) luaL_optnumber(L, 1 + i*4 + 4, defaultKandeloAlpha());
+			colors[i] = normalizeKandeloColor(colors[i]);
 		}
 
 		t->setColor(colors);
@@ -571,13 +609,14 @@ int w_ParticleSystem_getColors(lua_State *L)
 	{
 		lua_createtable(L, 4, 0);
 
-		lua_pushnumber(L, colors[i].r);
+		Colorf c = denormalizeKandeloColor(colors[i]);
+		lua_pushnumber(L, c.r);
 		lua_rawseti(L, -2, 1);
-		lua_pushnumber(L, colors[i].g);
+		lua_pushnumber(L, c.g);
 		lua_rawseti(L, -2, 2);
-		lua_pushnumber(L, colors[i].b);
+		lua_pushnumber(L, c.b);
 		lua_rawseti(L, -2, 3);
-		lua_pushnumber(L, colors[i].a);
+		lua_pushnumber(L, c.a);
 		lua_rawseti(L, -2, 4);
 	}
 
diff --git a/src/modules/graphics/wrap_SpriteBatch.cpp b/src/modules/graphics/wrap_SpriteBatch.cpp
index 9a9d0e4..8ea82fc 100644
--- a/src/modules/graphics/wrap_SpriteBatch.cpp
+++ b/src/modules/graphics/wrap_SpriteBatch.cpp
@@ -25,11 +25,48 @@
 #include "Canvas.h"
 #include "wrap_Texture.h"
 
+#ifdef LOVE_KANDELO
+extern "C" bool kandelo_love_uses_legacy_color_range();
+#endif
+
 namespace love
 {
 namespace graphics
 {
 
+static bool usesKandeloLegacyColorRange()
+{
+#ifdef LOVE_KANDELO
+	return kandelo_love_uses_legacy_color_range();
+#else
+	return false;
+#endif
+}
+
+static Colorf normalizeKandeloColor(Colorf c)
+{
+	if (!usesKandeloLegacyColorRange())
+		return c;
+
+	c.r /= 255.0f;
+	c.g /= 255.0f;
+	c.b /= 255.0f;
+	c.a /= 255.0f;
+	return c;
+}
+
+static Colorf denormalizeKandeloColor(Colorf c)
+{
+	if (usesKandeloLegacyColorRange())
+		c *= 255.0f;
+	return c;
+}
+
+static float defaultKandeloAlpha()
+{
+	return usesKandeloLegacyColorRange() ? 255.0f : 1.0f;
+}
+
 SpriteBatch *luax_checkspritebatch(lua_State *L, int idx)
 {
 	return luax_checktype<SpriteBatch>(L, idx);
@@ -184,7 +221,7 @@ int w_SpriteBatch_setColor(lua_State *L)
 		c.r = (float) luaL_checknumber(L, -4);
 		c.g = (float) luaL_checknumber(L, -3);
 		c.b = (float) luaL_checknumber(L, -2);
-		c.a = (float) luaL_optnumber(L, -1, 1.0);
+		c.a = (float) luaL_optnumber(L, -1, defaultKandeloAlpha());
 
 		lua_pop(L, 4);
 	}
@@ -193,9 +230,10 @@ int w_SpriteBatch_setColor(lua_State *L)
 		c.r = (float) luaL_checknumber(L, 2);
 		c.g = (float) luaL_checknumber(L, 3);
 		c.b = (float) luaL_checknumber(L, 4);
-		c.a = (float) luaL_optnumber(L, 5, 1.0);
+		c.a = (float) luaL_optnumber(L, 5, defaultKandeloAlpha());
 	}
 
+	c = normalizeKandeloColor(c);
 	t->setColor(c);
 
 	return 0;
@@ -211,6 +249,7 @@ int w_SpriteBatch_getColor(lua_State *L)
 	if (!active)
 		return 0;
 
+	color = denormalizeKandeloColor(color);
 	lua_pushnumber(L, color.r);
 	lua_pushnumber(L, color.g);
 	lua_pushnumber(L, color.b);
diff --git a/src/modules/image/wrap_ImageData.cpp b/src/modules/image/wrap_ImageData.cpp
index 70721cf..fb31ddf 100644
--- a/src/modules/image/wrap_ImageData.cpp
+++ b/src/modules/image/wrap_ImageData.cpp
@@ -29,11 +29,48 @@ static const char imagedata_lua[] =
 #include "wrap_ImageData.lua"
 ;
 
+#ifdef LOVE_KANDELO
+extern "C" bool kandelo_love_uses_legacy_color_range();
+#endif
+
 namespace love
 {
 namespace image
 {
 
+static bool usesKandeloLegacyColorRange()
+{
+#ifdef LOVE_KANDELO
+	return kandelo_love_uses_legacy_color_range();
+#else
+	return false;
+#endif
+}
+
+static Colorf normalizeKandeloColor(Colorf c)
+{
+	if (!usesKandeloLegacyColorRange())
+		return c;
+
+	c.r /= 255.0f;
+	c.g /= 255.0f;
+	c.b /= 255.0f;
+	c.a /= 255.0f;
+	return c;
+}
+
+static Colorf denormalizeKandeloColor(Colorf c)
+{
+	if (usesKandeloLegacyColorRange())
+		c *= 255.0f;
+	return c;
+}
+
+static float defaultKandeloAlpha()
+{
+	return usesKandeloLegacyColorRange() ? 255.0f : 1.0f;
+}
+
 /**
  * NOTE: Additional wrapper code is in wrap_ImageData.lua. Be sure to keep it
  * in sync with any changes made to this file!
@@ -97,6 +134,7 @@ int w_ImageData_getPixel(lua_State *L)
 
 	Colorf c;
 	luax_catchexcept(L, [&](){ t->getPixel(x, y, c); });
+	c = denormalizeKandeloColor(c);
 
 	lua_pushnumber(L, c.r);
 	lua_pushnumber(L, c.g);
@@ -117,7 +155,7 @@ int w_ImageData_setPixel(lua_State *L)
 	if (lua_istable(L, 4))
 	{
 		for (int i = 1; i <= components; i++)
-			lua_rawgeti(L, components, i);
+			lua_rawgeti(L, 4, i);
 
 		c.r = (float) luaL_checknumber(L, -components);
 		if (components > 1)
@@ -126,6 +164,6 @@ int w_ImageData_setPixel(lua_State *L)
 			c.b = (float) luaL_checknumber(L, (-components) + 2);
 		if (components > 3)
-			c.a = (float) luaL_optnumber(L, (-components) + 3, 1.0);
+			c.a = (float) luaL_optnumber(L, (-components) + 3, defaultKandeloAlpha());
 
 		lua_pop(L, components);
 	}
@@ -139,8 +177,9 @@ int w_ImageData_setPixel(lua_State *L)
 		if (components > 2)
 			c.b = (float) luaL_checknumber(L, 6);
 		if (components > 3)
-			c.a = (float) luaL_optnumber(L, 7, 1.0);
+			c.a = (float) luaL_optnumber(L, 7, defaultKandeloAlpha());
 	}
+	c = normalizeKandeloColor(c);
 
 	luax_catchexcept(L, [&](){ t->setPixel(x, y, c); });
 	return 0;
@@ -180,6 +219,7 @@ int w_ImageData__mapPixelUnsafe(lua_State *L)
 
 			Colorf c;
 			pixelgetfunction(pixeldata, c);
+			c = denormalizeKandeloColor(c);
 
 			lua_pushvalue(L, 2); // ImageData
 
@@ -200,8 +240,9 @@ int w_ImageData__mapPixelUnsafe(lua_State *L)
 			if (components > 2)
 				c.b = (float) luaL_checknumber(L, -2);
 			if (components > 3)
-				c.a = (float) luaL_optnumber(L, -1, 1.0);
+				c.a = (float) luaL_optnumber(L, -1, defaultKandeloAlpha());
 
+			c = normalizeKandeloColor(c);
 			pixelsetfunction(c, pixeldata);
 
 			lua_pop(L, 4); // Pop return values.
