script_path = "packages/registry/php/build-php.sh"
inputs = [
  "packages/registry/php/build-php.sh",
  "packages/registry/php/fork-side-module-abi.c",
  "packages/registry/php/intl-icu-data-loader.c",
  "scripts/install-local-binary.sh",
  "scripts/package-build-roots.sh",
  "scripts/wasm-artifact-guards.sh",
]
repo_url    = "https://github.com/Automattic/kandelo.git"
commit      = "UNPUBLISHED"
# Batch 3 extension landings advance this recipe once per byte-changing layer:
# zip (11), curl (12), intl/ICU (13), truthful unshare detection (14), then
# pdo_sqlite column metadata against sqlite revision 2 (15), then ABI identity
# for the fork-instrumented opcache side module (16), then make every dylink
# output carry complete ABI 43 side-boundary instrumentation (17).
revision    = 17

[binary]
index_url = "https://github.com/Automattic/kandelo/releases/download/binaries-abi-v{abi}/index.toml"
