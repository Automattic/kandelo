script_path = "packages/registry/spdlog/build-spdlog.sh"
repo_url    = "https://github.com/Automattic/kandelo.git"
commit      = "cf81d0f7c"
revision    = 1

# The fetched `spdlog-src/` tree is gitignored; the resolver hashes
# these tracked inputs on a clean checkout.
inputs = [
  "packages/registry/spdlog/build-spdlog.sh",
  "scripts/package-build-roots.sh",
]
