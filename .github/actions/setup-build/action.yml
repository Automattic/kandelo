name: setup-build
description: >
  Shared CI preamble for staging-build.yml jobs: submodules, Nix install +
  magic-nix-cache, resolver/cargo + sysroot caches, dev-shell warm-up, and
  optional restore of the build-outputs artifact uploaded by `prepare`.
  Callers must `actions/checkout@v4` first — local composite actions
  require the repo to already be checked out.

inputs:
  download-build-outputs:
    description: When 'true', restore the build-outputs artifact uploaded by `prepare`
    required: false
    default: 'false'
  merge-with-main:
    description: >
      When 'true', merge origin/main into the checked-out PR HEAD in-memory
      before any build steps. Used by prepare-merge.yml to test against the
      merged tree. If the calling job already merged (e.g. prepare did its
      own conflict-aware merge), this step is a no-op ("Already up to date").
    required: false
    default: 'false'

runs:
  using: composite
  steps:
    - name: Fetch submodules (musl, libc-test, os-test)
      # actions/checkout doesn't pull submodules by default. We need:
      #   musl       — for scripts/build-musl.sh
      #   libc-test  — for scripts/run-libc-tests.sh
      #   os-test    — for scripts/run-sortix-tests.sh
      # libc-test's URL in .gitmodules is git@github.com: (SSH);
      # rewrite to HTTPS for runner-side anonymous fetch.
      shell: bash
      run: |
        git config --global url."https://github.com/".insteadOf "git@github.com:"
        git submodule update --init --depth 1 musl libc-test os-test

    - name: Merge tip-of-main into PR HEAD (in-memory)
      if: inputs.merge-with-main == 'true'
      # Used by prepare-merge.yml so every job tests/builds against the
      # tree the squash merge will land. No conflict-specific handling
      # here — prepare-merge's `prepare` job runs an explicit merge step
      # earlier with the label-drop-and-comment behaviour, so any
      # conflict is already caught and reported by the time downstream
      # jobs reach this composite. If prepare's merge already ran, this
      # is a no-op ("Already up to date").
      shell: bash
      run: |
        git config user.name "wasm-posix-binaries-bot"
        git config user.email "noreply@anthropic.com"
        git fetch origin main
        git merge --no-edit origin/main

    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@v18

    - name: Cache Nix store + flake eval
      uses: DeterminateSystems/magic-nix-cache-action@v13

    - name: Cache resolver + cargo target
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/wasm-posix-kernel
          target
        key: staging-build-${{ runner.os }}-${{ hashFiles('Cargo.lock', 'examples/libs/**/deps.toml') }}
        restore-keys: |
          staging-build-${{ runner.os }}-

    - name: Cache musl sysroot + libc++
      uses: actions/cache@v4
      id: cache-sysroot
      with:
        path: |
          sysroot
          sysroot64
        # v3: also includes libc++ (needed for C++ packages like dinit
        # whose build scripts bail when sysroot/lib/libc++.a is missing).
        key: musl-sysroot-v3-${{ runner.os }}-${{ hashFiles('scripts/build-musl.sh', 'scripts/build-libcxx.sh', 'musl-overlay/**', 'glue/**') }}

    - name: Build musl sysroot + libc++ (wasm32 + wasm64)
      if: steps.cache-sysroot.outputs.cache-hit != 'true'
      # build-programs.sh emits wasm64 fixtures only when sysroot64
      # exists. C++ packages (dinit, mariadb-test, …) require
      # libc++.a + headers in the sysroot. The MariaDB build is the
      # canonical place where C++ headers get installed; replicate
      # the relevant part inline before build-libcxx.sh so dinit's
      # compile sees `#include <iostream>`.
      shell: bash
      run: |
        nix develop --accept-flake-config --command bash scripts/build-musl.sh
        nix develop --accept-flake-config --command bash scripts/build-musl.sh --arch wasm64posix
        nix develop --accept-flake-config --command bash -c '
          # Nix puts libc++ headers under share/libc++/v1 (vs.
          # Homebrew at include/c++/v1). Try both for portability.
          src=""
          for cand in "$LLVM_PREFIX/include/c++/v1" "$LLVM_PREFIX/share/libc++/v1"; do
            if [ -f "$cand/__config" ] || [ -f "$cand/iostream" ]; then
              src="$cand"; break
            fi
          done
          if [ -z "$src" ]; then
            echo "ERROR: cant locate libc++ headers under $LLVM_PREFIX" >&2
            exit 1
          fi
          echo "installing libc++ headers from $src"
          for arch_dir in sysroot sysroot64; do
            if [ ! -f "$arch_dir/include/c++/v1/__config" ]; then
              mkdir -p "$arch_dir/include/c++/v1"
              # cp -L dereferences symlinks (Nix symlinkJoin
              # produces a tree of symlinks into the read-only
              # store; without -L, later writes that overwrite
              # __config_site fail with EACCES).
              cp -RL "$src"/* "$arch_dir/include/c++/v1/"
              chmod -R u+w "$arch_dir/include/c++/v1/"
              cs="$arch_dir/include/c++/v1/__config_site"
              if [ -f "$cs" ]; then
                sed -i "s/_LIBCPP_HAS_MUSL_LIBC 0/_LIBCPP_HAS_MUSL_LIBC 1/" "$cs"
                sed -i "s/_LIBCPP_HAS_THREAD_API_PTHREAD 0/_LIBCPP_HAS_THREAD_API_PTHREAD 1/" "$cs"
              fi
            fi
          done
        '
        nix develop --accept-flake-config --command bash scripts/build-libcxx.sh
        nix develop --accept-flake-config --command bash scripts/build-libcxx.sh --arch wasm64

    - name: Warm Nix dev shell
      shell: bash
      run: nix develop --accept-flake-config --command true

    - name: Restore build-outputs artifact
      if: inputs.download-build-outputs == 'true'
      uses: actions/download-artifact@v4
      with:
        name: build-outputs
        path: .
