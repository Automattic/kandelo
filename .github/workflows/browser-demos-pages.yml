name: Deploy GitHub Pages

on:
  workflow_dispatch:
    inputs:
      source_sha:
        description: Exact main commit to deploy
        required: true
        type: string
      candidate_tag:
        description: Activated merge candidate selecting this generation
        required: true
        type: string
      canonical_index_sha256:
        description: Exact canonical package index digest
        required: true
        type: string

concurrency:
  group: kandelo-gh-pages
  cancel-in-progress: true

permissions:
  actions: read
  contents: write

env:
  # Exercise GitHub's post-Node-20 action runtime now, before it becomes
  # the runner default for actions that still declare node20.
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Validate the requested source generation
        env:
          EVENT_NAME: ${{ github.event_name }}
          REQUESTED_SOURCE_SHA: ${{ inputs.source_sha }}
          REQUESTED_CANDIDATE_TAG: ${{ inputs.candidate_tag }}
          REQUESTED_CANONICAL_INDEX_SHA256: ${{ inputs.canonical_index_sha256 }}
        run: |
          set -euo pipefail
          [ "$EVENT_NAME" = workflow_dispatch ]
          [[ "$REQUESTED_SOURCE_SHA" =~ ^[0-9a-f]{40}$ ]]
          [[ "$REQUESTED_CANDIDATE_TAG" =~ ^merge-candidate-abi-v[0-9]+-pr-[0-9]+-run-[0-9]+-attempt-[0-9]+$ ]]
          [[ "$REQUESTED_CANONICAL_INDEX_SHA256" =~ ^[0-9a-f]{64}$ ]]

      - name: Check out the source commit
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ inputs.source_sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Verify the checked-out source generation
        env:
          EVENT_NAME: ${{ github.event_name }}
          REQUESTED_SOURCE_SHA: ${{ inputs.source_sha }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          set -euo pipefail
          actual_source_sha=$(git rev-parse HEAD)
          [ "$EVENT_NAME" = workflow_dispatch ]
          [ "$actual_source_sha" = "$REQUESTED_SOURCE_SHA" ]
          git fetch --quiet --no-tags origin \
            "+refs/heads/$DEFAULT_BRANCH:refs/remotes/origin/$DEFAULT_BRANCH"
          [ "$(git rev-parse "refs/remotes/origin/$DEFAULT_BRANCH")" = \
            "$REQUESTED_SOURCE_SHA" ]

      - name: Fetch musl for repository-owned browser support programs
        uses: ./.github/actions/fetch-submodules
        with:
          submodules: libc/musl

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: |
            package-lock.json
            apps/browser-demos/package-lock.json
            host/package-lock.json

      - name: Install root dependencies
        run: npm ci --no-audit --no-fund

      - name: Install host dependencies
        working-directory: host
        run: PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm ci --no-audit --no-fund

      - name: Install browser demo dependencies
        working-directory: apps/browser-demos
        run: |
          npm ci --no-audit --no-fund
          npx playwright install chromium --with-deps

      - name: Typecheck host package
        working-directory: host
        run: npm run typecheck

      - name: Set up Nix
        uses: ./.github/actions/setup-nix

      - name: Verify the requested package generation
        id: package_generation
        env:
          GH_TOKEN: ${{ github.token }}
          EVENT_NAME: ${{ github.event_name }}
          REQUESTED_CANDIDATE_TAG: ${{ inputs.candidate_tag }}
          REQUESTED_CANONICAL_INDEX_SHA256: ${{ inputs.canonical_index_sha256 }}
        run: |
          set -euo pipefail
          abi=$(grep -oE 'ABI_VERSION: u32 = [0-9]+' \
            crates/shared/src/lib.rs | awk '{print $4}')
          canonical_tag="binaries-abi-v$abi"
          generation_dir="$RUNNER_TEMP/requested-package-generation"
          canonical_index="$generation_dir/index.toml"
          canonical_head="$generation_dir/head"
          mkdir -p "$generation_dir"
          bash scripts/release-index-state.sh snapshot \
            --target-tag "$canonical_tag" \
            --expected-abi "$abi" \
            --output "$canonical_index" \
            --head-file "$canonical_head"
          canonical_index_sha256=$(sha256sum "$canonical_index" | awk '{print $1}')
          source_sha=$(git rev-parse HEAD)

          [ "$EVENT_NAME" = workflow_dispatch ]
          [ "$canonical_index_sha256" = \
            "$REQUESTED_CANONICAL_INDEX_SHA256" ]
          [[ "$REQUESTED_CANDIDATE_TAG" =~ ^merge-candidate-abi-v${abi}-pr-[0-9]+-run-[0-9]+-attempt-[0-9]+$ ]]
          release_json="$generation_dir/candidate-release.json"
          gh api \
            "/repos/${{ github.repository }}/releases/tags/$REQUESTED_CANDIDATE_TAG" \
            > "$release_json"
          jq -e \
            --arg candidate_tag "$REQUESTED_CANDIDATE_TAG" '
                .tag_name == $candidate_tag and
                .draft == false and .immutable == true and
                .prerelease == true
            ' "$release_json" >/dev/null
          receipt_dir="$generation_dir/candidate"
          mkdir -p "$receipt_dir"
          gh release download "$REQUESTED_CANDIDATE_TAG" \
            --repo "${{ github.repository }}" \
            --pattern ready.json \
            --pattern activated.json \
            --dir "$receipt_dir"
          jq -e \
            --arg repository "${{ github.repository }}" \
            --arg candidate_tag "$REQUESTED_CANDIDATE_TAG" \
            --arg canonical_index_sha256 "$canonical_index_sha256" '
                .schema_version == 1 and
                .repository == $repository and
                .candidate_tag == $candidate_tag and
                .canonical_index_sha256 == $canonical_index_sha256 and
                (.merge_commit_sha | test("^[0-9a-f]{40}$")) and
                (.activated_at | type == "string" and length > 0) and
                (.activation_run | type == "string" and length > 0)
            ' "$receipt_dir/activated.json" >/dev/null
          jq -S \
            'del(.merge_commit_sha, .canonical_index_sha256, .activated_at, .activation_run)' \
            "$receipt_dir/activated.json" > "$generation_dir/activated-identity.json"
          jq -S . "$receipt_dir/ready.json" \
            > "$generation_dir/ready-identity.json"
          cmp "$generation_dir/activated-identity.json" \
            "$generation_dir/ready-identity.json"
          merge_commit_sha=$(jq -r .merge_commit_sha \
            "$receipt_dir/activated.json")
          git merge-base --is-ancestor "$merge_commit_sha" HEAD
          {
            echo "source_sha=$source_sha"
            echo "candidate_tag=$REQUESTED_CANDIDATE_TAG"
            echo "canonical_index_sha256=$canonical_index_sha256"
          } >> "$GITHUB_OUTPUT"

      - name: Verify browser package projection is current
        run: |
          bash scripts/dev-shell.sh bash -c '
            host_target="$(rustc -vV | sed -n "s/^host: //p")"
            source_root="$(pwd -P)"
            cargo run -p xtask --target "$host_target" --quiet -- \
              build-deps program-index-check \
              --source-repo-root "$source_root" \
              packages/registry packages/registry/program-packages.json
          '

      - name: Isolate canonical package resolution
        run: |
          set -euo pipefail
          product_cache="$RUNNER_TEMP/pages-canonical-package-cache"
          test ! -e "$product_cache"
          # WHY: an empty runner-local cache proves this deployment can recover
          # the complete canonical package generation. Reusing another job's
          # cache could hide an unavailable or stale public product archive.
          echo "WASM_POSIX_BINARY_CACHE_ROOT=$product_cache" >> "$GITHUB_ENV"

      - name: Prepare browser demo assets
        run: |
          set -euo pipefail
          # WHY: --fetch-only prevents a missing package archive from being
          # replaced by a source build. The current gallery still compiles its
          # repository-owned support-program bundle and derives VFS images;
          # neither path can replace the canonical shell bound below.
          bash scripts/dev-shell.sh env \
            "WASM_POSIX_BINARY_CACHE_ROOT=$WASM_POSIX_BINARY_CACHE_ROOT" \
            ./run.sh --fetch-only prepare-browser

      - name: Bind canonical package images
        id: package_products
        run: |
          set -euo pipefail
          shell_image=$(bash scripts/resolve-binary.sh programs/shell.vfs.zst)
          node_image=$(bash scripts/resolve-binary.sh programs/node-vfs.vfs.zst)
          bootstrap=$(bash scripts/resolve-binary.sh programs/homebrew-bootstrap/homebrew-bootstrap.zip)
          report="$RUNNER_TEMP/canonical-lazy-shell-report.json"
          for path in "$shell_image" "$node_image" "$bootstrap"; do
            test -f "$path"
            test ! -L "$path"
          done
          cmp "$bootstrap" apps/browser-demos/public/homebrew-bootstrap.zip
          test ! -e "$report"
          # WHY: resolver identity alone does not prove the intended lazy
          # partition or its exact checked-in public mirror binding.
          bash scripts/dev-shell.sh npx tsx --test \
            scripts/inspect-homebrew-main-shell-public-product.test.ts
          bash scripts/dev-shell.sh npx tsx \
            scripts/inspect-homebrew-main-shell-public-product.ts \
              --image "$shell_image" \
              --homebrew-bootstrap-archive "$bootstrap" \
              --homebrew-bootstrap-spec \
                homebrew/main-shell-brew-package-tree.json \
              --selection homebrew/main-shell-flat-selection.json \
              --materialization-policy \
                homebrew/main-shell-materialization-policy.json \
              --runtime-support-policy \
                homebrew/main-shell-runtime-support-policy.json \
              --mirror-plan \
                homebrew/main-shell-flat-lazy-mirror-plan.json \
              --out "$report"
          jq -e '
            .schema == 1 and
            .kind == "kandelo-homebrew-main-shell-public-product" and
            .partition == {
              embedded_bottles: 3,
              bootstrap_trees: 1,
              runtime_cohort_bottles: 2,
              ordinary_deferred_bottles: 35,
              deferred_bottles: 37,
              initial_pending_trees: 38
            } and
            (.image.sha256 | test("^[0-9a-f]{64}$")) and
            (.image.bytes > 0) and
            .bottle_mirror.asset_count == 37
          ' "$report" >/dev/null
          {
            echo "shell_image=$shell_image"
            echo "node_image=$node_image"
            echo "bootstrap=$bootstrap"
            echo "shell_sha256=$(jq -er '.image.sha256' "$report")"
            echo "node_sha256=$(sha256sum "$node_image" | awk '{print $1}')"
            echo "bootstrap_sha256=$(jq -er '.homebrew_bootstrap.sha256' "$report")"
            echo "bootstrap_bytes=$(jq -er '.homebrew_bootstrap.bytes' "$report")"
            echo "mirror_plan_url=$(jq -er '.bottle_mirror.plan_url' "$report")"
          } >>"$GITHUB_OUTPUT"

      - name: Verify the public Homebrew bottle mirror
        run: |
          set -euo pipefail
          env -u GH_TOKEN -u GITHUB_TOKEN node \
            scripts/verify-public-homebrew-bottle-mirror.mjs \
              --plan homebrew/main-shell-flat-lazy-mirror-plan.json \
              --out "$RUNNER_TEMP/pages-homebrew-mirror-receipt.json"

      - name: Build browser demos for GitHub Pages
        working-directory: apps/browser-demos
        env:
          VITE_BASE: /kandelo/
          VITE_CORS_PROXY_URL: https://wordpress-playground-cors-proxy.net/?
        run: |
          set -euo pipefail
          npm run build
          # WHY: Vite imports resolver-selected package images under hashed
          # asset names. Verify the exact emitted shell and Node bytes; an
          # unhashed public copy is not publication authority.
          bash ../../scripts/verify-browser-shell-vfs-asset.sh \
            dist "${{ steps.package_products.outputs.shell_image }}"
          bash ../../scripts/verify-browser-shell-vfs-asset.sh \
            dist "${{ steps.package_products.outputs.node_image }}" node-vfs.vfs
          cmp "${{ steps.package_products.outputs.bootstrap }}" \
            dist/homebrew-bootstrap.zip

      # The root publisher replaces the complete branch so content-addressed
      # browser assets cannot accumulate forever. Include the independently
      # published documentation trees in that replacement.
      - name: Build user guide for the complete Pages tree
        env:
          VITEPRESS_BASE: /kandelo/guide/
        run: |
          set -euo pipefail
          # WHY: documentation checks belong to the Pages publication path.
          # Adding them to the repository-wide package scripts changes the
          # cache identity of unrelated Kandelo runtime packages.
          node --test docs-site/.vitepress/homebrew-doc-links.test.mjs
          npm run docs:build
          node --test docs-site/.vitepress/homebrew-doc-output.test.mjs

      - name: Typecheck API docs for the complete Pages tree
        working-directory: host
        run: npx tsc --noEmit -p tsconfig.docs.json

      - name: Build API docs for the complete Pages tree
        working-directory: host
        run: npx typedoc

      - name: Add documentation to the complete Pages tree
        run: |
          cp -R docs-site/.vitepress/dist apps/browser-demos/dist/guide
          cp -R host/docs apps/browser-demos/dist/api

      - name: Record the deployed generation
        env:
          SOURCE_SHA: ${{ steps.package_generation.outputs.source_sha }}
          CANDIDATE_TAG: ${{ steps.package_generation.outputs.candidate_tag }}
          CANONICAL_INDEX_SHA256: ${{ steps.package_generation.outputs.canonical_index_sha256 }}
        run: |
          set -euo pipefail
          jq -n \
            --arg source_sha "$SOURCE_SHA" \
            --arg candidate_tag "$CANDIDATE_TAG" \
            --arg canonical_index_sha256 "$CANONICAL_INDEX_SHA256" '
              {
                schema_version: 1,
                source_sha: $source_sha,
                candidate_tag:
                  (if $candidate_tag == "" then null else $candidate_tag end),
                canonical_index_sha256: $canonical_index_sha256
              }
            ' > apps/browser-demos/dist/kandelo-deployment.json

      - name: Boot the canonical lazy Pages shell in Chromium
        working-directory: apps/browser-demos
        env:
          VITE_BASE: /kandelo/
          KANDELO_BROWSER_DEMO_INPUTS: main
          KANDELO_HOMEBREW_MAIN_SHELL_STRICT: "1"
          KANDELO_HOMEBREW_MAIN_SHELL_SHA256: ${{ steps.package_products.outputs.shell_sha256 }}
          KANDELO_HOMEBREW_MAIN_SHELL_BOOTSTRAP_SHA256: ${{ steps.package_products.outputs.bootstrap_sha256 }}
          KANDELO_HOMEBREW_MAIN_SHELL_BOOTSTRAP_BYTES: ${{ steps.package_products.outputs.bootstrap_bytes }}
          KANDELO_HOMEBREW_MAIN_SHELL_TRANSPORT_MODE: public
          KANDELO_HOMEBREW_MAIN_SHELL_MIRROR_PLAN_URL: ${{ steps.package_products.outputs.mirror_plan_url }}
          KANDELO_PLAYWRIGHT_SERVE_DIST: "1"
          KANDELO_TEST_BASE_URL: http://127.0.0.1:5401/kandelo/
        run: |
          set -euo pipefail
          bash ../../scripts/dev-shell.sh env \
            "WASM_POSIX_BINARY_CACHE_ROOT=$WASM_POSIX_BINARY_CACHE_ROOT" \
            "VITE_BASE=$VITE_BASE" \
            "KANDELO_BROWSER_DEMO_INPUTS=$KANDELO_BROWSER_DEMO_INPUTS" \
            "KANDELO_HOMEBREW_MAIN_SHELL_STRICT=$KANDELO_HOMEBREW_MAIN_SHELL_STRICT" \
            "KANDELO_HOMEBREW_MAIN_SHELL_SHA256=$KANDELO_HOMEBREW_MAIN_SHELL_SHA256" \
            "KANDELO_HOMEBREW_MAIN_SHELL_BOOTSTRAP_SHA256=$KANDELO_HOMEBREW_MAIN_SHELL_BOOTSTRAP_SHA256" \
            "KANDELO_HOMEBREW_MAIN_SHELL_BOOTSTRAP_BYTES=$KANDELO_HOMEBREW_MAIN_SHELL_BOOTSTRAP_BYTES" \
            "KANDELO_HOMEBREW_MAIN_SHELL_TRANSPORT_MODE=$KANDELO_HOMEBREW_MAIN_SHELL_TRANSPORT_MODE" \
            "KANDELO_HOMEBREW_MAIN_SHELL_MIRROR_PLAN_URL=$KANDELO_HOMEBREW_MAIN_SHELL_MIRROR_PLAN_URL" \
            "KANDELO_PLAYWRIGHT_SERVE_DIST=$KANDELO_PLAYWRIGHT_SERVE_DIST" \
            "KANDELO_TEST_BASE_URL=$KANDELO_TEST_BASE_URL" \
            npx playwright test \
            test/kandelo-homebrew-main-shell.spec.ts \
            --project=chromium

      - name: Run exact Pages Node npm acceptance
        working-directory: apps/browser-demos
        env:
          VITE_BASE: /kandelo/
          # The Node demo is selected by `?demo=node` inside the main page;
          # `node` is not a standalone Vite HTML input.
          KANDELO_BROWSER_DEMO_INPUTS: main
          KANDELO_NODE_VFS_STRICT: "1"
          KANDELO_NODE_VFS_SHA256: ${{ steps.package_products.outputs.node_sha256 }}
          KANDELO_PLAYWRIGHT_SERVE_DIST: "1"
          KANDELO_TEST_BASE_URL: http://127.0.0.1:5401/kandelo/
        run: |
          set -euo pipefail
          bash ../../scripts/dev-shell.sh env \
            "WASM_POSIX_BINARY_CACHE_ROOT=$WASM_POSIX_BINARY_CACHE_ROOT" \
            "VITE_BASE=$VITE_BASE" \
            "KANDELO_BROWSER_DEMO_INPUTS=$KANDELO_BROWSER_DEMO_INPUTS" \
            "KANDELO_NODE_VFS_STRICT=$KANDELO_NODE_VFS_STRICT" \
            "KANDELO_NODE_VFS_SHA256=$KANDELO_NODE_VFS_SHA256" \
            "KANDELO_PLAYWRIGHT_SERVE_DIST=$KANDELO_PLAYWRIGHT_SERVE_DIST" \
            "KANDELO_TEST_BASE_URL=$KANDELO_TEST_BASE_URL" \
            npx playwright test test/kandelo-node.spec.ts \
            --grep '@node-npm-acceptance' \
            --project=chromium

      - name: Enforce the GitHub Pages published-site size limit
        run: node scripts/check-pages-publish-size.mjs apps/browser-demos/dist 1000000000

      # Concurrency removes the usual superseded run promptly, but GitHub does
      # not guarantee the order in which runs enter a concurrency group. Query
      # this workflow immediately before its only publishing step so a delayed
      # older run cannot overwrite the newest triggered complete-tree build.
      - name: Confirm this is the newest Pages run
        id: publish_freshness
        env:
          GH_TOKEN: ${{ github.token }}
        run: bash scripts/check-pages-run-freshness.sh

      - name: Deploy to gh-pages
        if: steps.publish_freshness.outputs.publish == 'true'
        uses: peaceiris/actions-gh-pages@84c30a85c19949d7eee79c4ff27748b70285e453 # v4.1.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: apps/browser-demos/dist
          force_orphan: true
