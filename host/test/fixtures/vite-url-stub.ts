// Stub for Vite's `?url` and `?worker&url` imports so vitest can load
// modules that use them outside of Vite (e.g. BrowserKernel in
// host/test/browser-kernel.test.ts).
export default "stub://vite-url";
