{
  "$comment": [
    "Ratcheted ceilings on the surfaces this campaign exists to shrink.",
    "",
    "WHY THIS FILE EXISTS. The campaign produced 132 plan documents and one",
    "mechanism that never failed: EXPECTED_HOST_IMPORT_COUNT, a number in the",
    "repo asserted against a measurement of the built artifact. Every import",
    "change hit it. Meanwhile fork TypeScript grew by 2,867 lines over the same",
    "period with nothing to catch it, and nobody noticed for weeks. Documents",
    "are advisory; a number a test asserts is not.",
    "",
    "HOW THE RATCHET WORKS. Exceeding a ceiling fails: the surface grew.",
    "Falling more than `slack` below it ALSO fails, asking you to lower the",
    "ceiling in the same commit. That is what banks a reduction permanently --",
    "without it, one lane's deletion silently funds the next lane's growth.",
    "",
    "WHAT A CEILING IS NOT. It is not a goal. Every `target` below is far under",
    "its ceiling, and the gap is the work. See docs/plans/2026-09-11-MASTER-PLAN.md.",
    "",
    "LANE CLOSURE. `lanes` below binds each lane's status to measurements. A lane",
    "marked `closed` whose conditions are unmet fails; a lane marked `open` whose",
    "conditions are all met ALSO fails, saying to close it. Lane status therefore",
    "cannot be asserted in prose -- which is what every previous attempt did, and",
    "why drift was only ever caught when a human happened to ask.",
    "",
    "A lane whose completion is not a number says so with `manual` and a reason.",
    "That is honest; silence is not.",
    "",
    "CHECKLIST CLOSURE is weaker than a measurement and is labelled as such. A",
    "tick is self-reported. Its value is that the instances are enumerated, so a",
    "lane cannot be closed by forgetting one.",
    "",
    "CHARACTERIZATION is gated too. A lane must carry an End state, a floor, an",
    "Increments list, Acceptance evidence and Known hazards before it is",
    "dispatchable. A lane legitimately missing one declares `sectionsExempt` with",
    "a reason -- because the failure this file exists to end is a status asserted",
    "in prose that nothing checks, and that applies to its own sections first.",
    "",
    "MEASURE CHANGE 2026-09-12 -- the seven TypeScript line surfaces count CODE",
    "lines (neither blank nor purely a comment), not total lines. They exist to",
    "stop TypeScript IMPLEMENTATION growing, because the goal is to move",
    "implementation into Rust and the fork-module, and a total-line ceiling cannot",
    "tell an added function from an added paragraph of explanation.",
    "",
    "It failed a real commit that did the right thing: lane F's fm_capture_intern",
    "collapse removed four per-type module exports and three host marshalling",
    "wrappers -- fork TypeScript CODE fell 5 lines -- and the gate rejected it",
    "because the commit also explained the collapse in 20 lines of comment. The",
    "first response was to compress the explanation until the number went down.",
    "That is the metric shaping the code instead of measuring it, so the metric",
    "changed. A comment now costs nothing, which is the intended incentive: this",
    "campaign's expensive defects are consistently the ones nobody wrote down.",
    "",
    "Ceilings and targets were re-baselined in the same change. Targets were",
    "scaled by each surface's own code/total ratio (~0.80), so a target keeps the",
    "meaning it had: what fraction of this surface should survive. Targets of 0",
    "stay 0."
  ],
  "surfaces": {
    "forkTypeScript": {
      "measure": "code lines (non-blank, non-comment) of host/src/fork-module-*.ts",
      "ceiling": 811,
      "slack": 0,
      "target": 484,
      "why": "The MODULE-FACING half of stage 2: everything that places, instantiates and talks to the co-resident fork-module. BANKED at its measurement 2026-09-12 by maintainer decision, replacing the headroom ceiling of 250. That headroom was approved for exactly this half and the half is complete -- fork-module-host-capabilities.ts (65) plus fork-module-instance.ts (184) -- so leaving the ceiling above what exists would be the unbanked slack this file's preamble says funds the next lane's growth. SCOPE NARROWED at the same time: this used to measure every host/src/fork-*.ts and vfork-*.ts, which mixed a finished population with an unstarted one, and a ceiling over both cannot be read. The platform half now has its own surface, forkPlatformTypeScript, for the same reason forkModuleEntryPoints was split by purpose (see docs/plans/2026-09-12-lane-f-census.md section 10). TARGET EQUALS CEILING because this half is done: it is a floor to hold, not a gap to close. Every entry it contains is argued as a Wasm capability limit in the file itself -- placement cannot move into a side module, which does not choose where it is placed. Grounded against crates/host-native, which does the same work in 142 code lines (census sections 11, 15, 17 and 18).\n\nRAISED 249 -> 419 on 2026-09-13 for `host/src/fork-module-backend.ts`, and the raise says something about the old number: this surface was declared DONE with target == ceiling while its LARGEST member sat in the attic. 249 measured an incomplete population, the same structural error `forkPlatformTypeScript`'s glob had. The backend is the module-facing half by definition -- it is the host's calls into the co-resident module. \n\nWhat it replaces is the argument for the number: 170 code lines against the 1239-line wrapper it supersedes, an 86% reduction. Two things shrank it -- the module folded eleven counter exports into one `fm_stats(field)` and the host wrapper never followed, so eight one-line accessors became one `stat()`; and half the old surface served the capture/replay orchestration the module took over. Methods come back when a caller needs one. \n\nTarget equals ceiling again, deliberately: this is a floor to hold. If it rises without a caller appearing in `worker-main.ts`, that is the anticipation the 1239 lines were made of.\n\nBANKED 419 -> 418 on 2026-09-13, the same day the maintainer set 419. The table-mutation commit needed a `channelBase` option (+2 lines, because a borrowed fork child cannot derive its channel from the archive control address). Rather than raise a just-decided ceiling by two, `stat()` lost its local 'is that a real field?' check: `fm_stats` answers -1 for a field it does not have, and the module is the authority on which fields exist. That check was a second opinion on the same question -- the duplication this lane removed from the GC codec and the exception codec -- so it should not have been there. Net -1.\n\nRAISED 418 -> 433 on 2026-09-13, for two things the maintainer-approved `__wpk_fork_host_func_identity` made necessary. Three lines bind it in `instantiateForkModule`. The other twelve are a COMPLETENESS CHECK: before instantiating, every `env` function the artifact imports must be bound, or the host fails with the import's NAME. \n\nThat check is here because its absence was a real defect, not a hypothetical. Adding the import broke this file's own tests, and the failure arrived as `LinkError: Import #10 ... requires a callable` -- an index, not a name. The guest side of the same contract has been complete by construction since `buildForkGuestImports`; the module's own host obligation had no equivalent, so the two directions were held to different standards. \n\nPlaced BEFORE instantiation deliberately: the first version ran after, where the `LinkError` has already fired and the check can never speak. Perturbing it by removing the binding now fails with 'does not bind: __wpk_fork_host_func_identity'.\n\nRAISED 433 -> 457 on 2026-09-13 for `sealCaptureAndSerialize` and a lazy `exports` getter, and the reason is a correction to how this file was rewritten. The rewrite's rule was 'methods come back when a caller needs one' -- and I checked only `worker-main.ts` for callers. \n\nA TEST is a caller. `fork-module-backend-coarse-failures` exercises the seal path specifically to prove a failed seal raises a TYPED `ContinuationAllocationError` rather than a generic throw that traps the worker. That test was failing and I had not noticed, because the gated runs used a hand-picked file list. Its absence from `worker-main` proves nothing: worker-main's caller for it is in the attic'd cluster, so 'no caller' was an artifact of the cluster being down. \n\nThe lazy getter is the other half: `exports` is now read on demand so the resume-catalog cap check genuinely runs BEFORE any export is touched, which is what that same test asserts and what lets it prove the boundary with a stand-in instance.\n\nRAISED 457 -> 484 on 2026-09-13 for `bindActivationDrive`, the second piece of the cluster port. It replaces `forkGcCodecProviderFromInstance` (about 90 attic lines) with 27: the old one bound five guest exports into a JavaScript object the HOST then called, and the module calls them now, so a host only has to put them where the module can reach -- a reference-typed `Table.set`, which is a genuine floor because Rust cannot hold a funcref. \n\nThe five slot offsets are duplicated from `fork-codec`'s `drive_plan` and pinned against it, because binding the wrong slot makes the module call the wrong guest function with arguments that look right -- an allocate driven as a fill. Nothing traps; the child is simply rebuilt wrong. The stride is pinned for the same reason: a host growing by a smaller one leaves later activations overlapping earlier ones.\n\nRAISED 484 -> 513 on 2026-09-13 for `capturedExternrefHandles` and `stageExternrefHandover`, both with production callers in `worker-main.ts` from the same commit -- the anticipation test this surface's rationale sets. They read the module's record of interned externref handles and stage it for the kernel worker.\n\nRAISED 513 -> 672 on 2026-09-13, and this one is a RELOCATION rather than growth: `fork-reference-capture-module` moves here from `forkRestoredHostFloor`, which falls 4231 -> 4072 in the same commit. Net zero across the two. The file is module-facing by its own description -- \"a thin, stateful wrapper over the `fm_capture_*` exports of ONE resident fork-module instance\" -- and the restored-floor surface is defined as process lifecycle, cross-worker transport or memory placement RATHER THAN capture/replay logic. Leaving a `fork-*.ts` filename glob to decide would have left that surface 159 lines above a ceiling argued to move only for reclassifications, and the obvious fix would have been raising the wrong one.\n\nPROVISIONAL RAISE 672 -> 703 on 2026-09-14 for the host half of `fm_borrowed_replay_workspace`: `borrowedReplayWorkspace()` and the `borrowedReplayWorkspaceOf` null guard in `fork-module-backend.ts`, which is where the host's calls into the co-resident module live. It buys the removal of a coordinator method whose JS body walked activation formats and capture-session internals; the line count of what it replaces does not come off this surface, because that body is in the attic and the attic is measured by nothing. Worth saying plainly: this surface's ledger will look like pure growth for the whole coordinator cut, and the reduction only appears when worker-main.ts stops importing the attic at all.\n\nPROVISIONAL RAISE 703 -> 718 on 2026-09-14 for the other side of that consolidation: `FORK_ACTIVATION_DRIVE_BINDINGS` grew from five entries to thirteen, and each entry gained a `required` flag. The five it had bound were the typed-reconstruction slots, which is why the JS host could drive reconstruction and nothing else -- the unwind/rewind/abort quartet the entire fork lifecycle runs on was never bound by this host at all, and an unbound drive slot is a `call_indirect` on null rather than a missing feature. Net across both surfaces is -33 host code lines, and the 15 that landed here are table entries under a test that now also asserts no slot in the stride is left unbound.\n\nPROVISIONAL RAISE 718 -> 719 on 2026-09-14. One line: the drive-table binding entry for `wpk_fork_module_state_save`, the capture-side guest walk that writes an activation's globals and tables into the KFMS arena. The module could drive the child's RESTORE and never the parent's SAVE -- no drive op existed for it -- so a module-sequenced capture produced an arena with no global or table records in it, silently. This is the host half of closing that.\n\nPROVISIONAL RAISE 719 -> 742 on 2026-09-14 for the four lifecycle methods the backend was missing: `parentBeginCapture`, `parentReplay`, `parentFinish`, `abort`. They existed on the 1,239-line wrapper this file replaced and went when it was cut to 20 methods, on the stated principle that \"methods come back when a caller needs one\". A caller needs them: `worker-main.ts` is being moved to its end state, where the fork lifecycle is four module calls instead of a JS coordinator, and this is that surface. Each is a call plus an errno check; all four are driven against a live module by `host/test/fork-module-capture-drive.test.ts` before anything in production calls them.\n\nBANKED 742 -> 740 on 2026-09-14: `borrowedReplayWorkspaceOf`, a null guard written for exactly one method, became `requireForkModuleBackend` serving all of them. It stopped being the right shape the moment a second caller needed it.\n\nPROVISIONAL RAISE 740 -> 743 on 2026-09-14 for `parentAbortSeal`, the mid-unwind failure path. Distinct from `sealCaptureAndSerialize` because it must NOT drive the guest's unwind-end: the guest is still mid-unwind there and driving it corrupts the unwind state machine. Paired with removing the coordinator's `beginModuleCaptureAbort` in the same commit, and driven against a live module before the host depends on it.\n\nPROVISIONAL RAISE 743 -> 757 on 2026-09-14 for `attachChild` and `driveRestoredPlan`, which give `fm_attach_child` its FIRST production caller -- it had none through the whole campaign. One install call serves both child shapes, because a COW child and a vfork borrowed child share an identical plan in the module and the only borrowed-specific work is a host-side prefix reservation that never entered it. `driveRestoredPlan` reads the step count from the module rather than taking it from the caller, so the two cannot disagree about how much of a plan to run. Paired with removing the coordinator's `attachChild`/`attachBorrowedChild` in the same commit, taking it from 6 call sites to 4.\n\nPROVISIONAL RAISE 757 -> 789 on 2026-09-14 for the three import seeds: `setActivationImports`, `setIdentityGroup` and `setImportProvenance`. They are the calls through which `host/src/fork-import-identity.ts` publishes what only JavaScript can see about an activation's imports -- the sections the module cannot extract, which objects are the same object, and what each imported value turned out to be. Each is a stage-and-call, and `call` widened to accept a bigint because a raw scalar import's bits are an i64. Paired with the new host file rather than standing alone; see `forkPlatformTypeScript` for what the pair replaces.\n\nPROVISIONAL RAISE 789 -> 801 on 2026-09-14 for `setActivationTableStateOwner`, which gives `host/src/fork-table-state-owners.ts` its first production caller -- it had none since it was written. It publishes which coordinate WRITES a shared table's sparse state, and it replaces `aliasTableDirtyTrackers`, a JS pass over every activation's dirty-journal object that ran after the fact. The election now happens as each activation completes, and the journal it used to alias is the module's.\n\nPROVISIONAL RAISE 801 -> 811 on 2026-09-14: `parentBeginCapture` now takes the side activations as a list and stages them itself. Where the `(id, fixedPrefix)` pairs are written in guest memory is this wrapper's business; the caller should be naming activations, not laying out bytes. The ten lines here are the layout worker-main does not have."
    },
    "workerMainTypeScript": {
      "measure": "code lines (non-blank, non-comment) of host/src/worker-main.ts",
      "ceiling": 5793,
      "slack": 150,
      "target": 2400,
      "why": "Holds the fork drive loop. 6,083 -> 7,536 across the campaign; every fine-grained fm_* call leaves a host-side seeding and base-arithmetic loop behind by construction. Re-baselined 2026-09-12 from total lines to CODE lines: ceiling 7499 -> 5958, target 3000 -> 2400. Same code, a measure that counts implementation.\n\nBANKED 5958 -> 5906 on 2026-09-13. The cluster port removed the host's two remaining decodes of module-owned formats from this file: the GC and exception codec DESCRIPTORS. Neither was needed -- the host already locates both sections and hands the module their raw bytes, and the host-exception owner (the smallest activation that DECLARED an exception codec) falls out of which activations have a section, not out of a decoded descriptor. \n\nThis is the direction this file is supposed to move, so the reduction is banked rather than left as slack for the next addition to spend.\n\nBANKED 5906 -> 5899 on 2026-09-13. The main program's module-state descriptor re-validation left `worker-main.ts`: `check_module_state` in crates/wasm-artifact already makes the identical pointer-width comparison, on an artifact that reached that line only by passing the artifact policy during exec. Seven lines, and the check that remains is the better one -- it names the field that failed. The dlopen side-activation copy STAYS, because the artifact policy is never asked about a side module (census section 103).\n\nBANKED 5899 -> 5858 on 2026-09-13. All THREE fork `env` construction sites now build their imports with `buildForkGuestImports` instead of `buildForkActivationStateImports` + `buildForkExceptionImports`, and both attic builders are gone from this file. The replacement is net-negative only because the binder made four existing override blocks redundant: it binds the module's exports for every contract name, so the per-site re-binding of `__wpk_fork_frame_{commit,peek,next}`, `__wpk_fork_resume_peek`, the two `__wpk_fork_ref_decode_*` flips and the whole `forkModuleReferenceFlip` option were re-assigning values already assigned.\n\nOne of those blocks was not merely redundant, it was BROKEN: `moduleReferenceFeedFlip` bound seven guest imports to `forkModuleInstance.exports.fm_ref_*`, and the module exports the GUEST spellings (`__wpk_fork_ref_vector_get`), not the `fm_ref_*` ones -- so every one of those seven was `undefined` whenever the flip fired. See census section 111.\n\nBANKED 5858 -> 5845 on 2026-09-13, the plumbing that DIED with the import builders rather than anything newly written. `referenceReplay` existed to feed them and had no other consumer, taking its `currentReferences()` call with it; `ProcessReferenceReplayImports` was referenced only by its own declaration, and deleting it removed the two attic TYPE imports it existed to combine; the `exceptionBroker` option was still being supplied at two sites and read at none. Attic symbols imported by worker-main: 30 -> 28.\n\nRAISED 5845 -> 5858 on 2026-09-13 for the externref handover write: after seal and before the fork syscall, the parent stages the handles its capture interned and records the address and count in the host-private control prefix. Thirteen lines here replace the kernel worker decoding this worker's parked arena to learn the same thing.\n\nPROVISIONAL RAISE 5858 -> 5859 on 2026-09-14, and it is worth saying what the one line IS: an import statement. Both call sites the `fm_phase` change touches were rewritten as one-line-for-one-line swaps precisely so this file would not grow, and it grew by the import anyway. That is worth noticing about the NUMBER rather than the change: a ceiling banked at the exact measurement means this file can never import anything again, which cannot be the rule intended. Flagged for the maintainer with the other two.\n\nPROVISIONAL RAISE 5859 -> 5860 on 2026-09-14. Net of one: the attic type import ForkBorrowedReplayWorkspaceRequirements came OFF (fork-process-continuation now supplies one symbol fewer), and two lines went on -- the module-backend import of its replacement type and the guard function. Both call sites are one-line-for-one-line.\n\nBANKED 5860 -> 5812 on 2026-09-14, and it is the first real reduction this lane has taken out of this file. Two inline drive-table binding loops -- 73 lines of slot arithmetic, table growth and per-export presence checks -- collapsed into one call to `ForkModuleContinuationBackend.bindActivationDrive`. The binding logic did not move to a new home; it moved to a method that already existed, already had a slot-by-slot pin test against `fork_codec::drive_plan`, and had NO callers at all. The reduction is banked rather than left as slack, per this file's own rule.\n\nRAISED 5812 -> 5818 on 2026-09-14, and the honest accounting matters more than the number. Nine coordinator call sites came out (22 -> 13): the lifecycle quartet, both aborts, both abort-guard phase reads, and `abortErrno`. Every replacement is one line for one line. The +6 is a narrowed-backend accessor and the `forkAbortErrno` the coordinator used to hold. So this file grew while getting closer to done, because what it stopped depending on lives in the attic and no surface counted that until `forkAtticImports` was added in this commit.\n\nRAISED 5818 -> 5823 on 2026-09-14. Four more coordinator uses gone (11 -> 7): `continuationImports`, `registerActivation`, `unregisterActivation` and `clear` now go through `ForkResumeTable`, which already existed in host/src as the thin floor for exactly this and had NO callers -- dead floor written ahead of its consumer, which is the mistake this lane keeps having to unlearn. The +5 is the table's construction and the two-line import expression that replaced a one-line coordinator call. The resume table is genuinely host floor: a live WebAssembly.Table holding the guest's own exported thunks, which the module cannot hold for it.\n\nRAISED 5823 -> 5824 on 2026-09-14. The coordinator's `beginModuleCaptureAbort` became three explicit steps at the call site -- record the errno, seal the partial capture, replay it as an abort -- which is one line more and no longer hides the sequence inside a method whose name did not say it replayed."
    },
    "sffsTypeScript": {
      "measure": "code lines (non-blank, non-comment) of host/src/vfs/sharedfs-vendor.ts",
      "ceiling": 3047,
      "slack": 100,
      "target": 0,
      "why": "The SFFS block filesystem is implemented twice: 3,752 lines here against 3,727 in sffs.rs + sffs_write.rs + sffs_deferred.rs, same superblock geometry, same allocators, same magic. Target is zero because one of the two must go and the kernel owns the format. Re-baselined 2026-09-12 from total lines to CODE lines: ceiling 3716 -> 3047, target 0 -> 0. Same code, a measure that counts implementation."
    },
    "memoryFsTypeScript": {
      "measure": "code lines (non-blank, non-comment) of host/src/vfs/memory-fs.ts",
      "ceiling": 7473,
      "slack": 200,
      "target": 0,
      "why": "The other half of the 12,198-line TypeScript filesystem the lane V text names and no register measured. MemoryFileSystem backs `/` on every machine and implements stat, statfs, pathconf, dirents, open flags and ST_NOSUID setuid handling in the host, against 1,724 lines of tmpfs.rs in the kernel. Target is 0 because lane V's end state says the host supplies nothing inside the image body; the V6 census is the deliverable that would prove any of it is floor, and until that census runs the 0 is an assertion of the plan's end state, not a measured floor. If the census finds real floor here, this target gets revised in the open with that evidence attached. Re-baselined 2026-09-12 from total lines to CODE lines: ceiling 8501 -> 7473, target 0 -> 0. Same code, a measure that counts implementation."
    },
    "hostImportFunctions": {
      "measure": "env.host_* function imports of the built kernel; see EXPECTED_HOST_IMPORT_COUNT",
      "ceiling": 72,
      "slack": 0,
      "target": 40,
      "why": "V4, the primary goal: how many functions a new host must implement. Already asserted against the built artifact in crates/host-native; restated here so one file carries every surface. A raw import-ENTRY count reads 73 because env.memory is an entry -- say which you counted."
    },
    "sffsModuleEntryPoints": {
      "measure": "pub extern \"C\" fn sm_* declared in crates/sffs-module/src/lib.rs",
      "ceiling": 19,
      "slack": 0,
      "target": 19,
      "why": "Banked at its measured value the moment it reached 19, not left to grow unwatched. This is the image builders' ABI, and every entry point is a call a builder makes and a shape the module must keep. The target is NOT 0: unlike forkModuleEntryPoints, these are not fine-grained steps of one operation that a coarse entry could replace -- they are distinct filesystem operations a builder genuinely performs, and the census measured 24 MemoryFileSystem methods that 19 already cover. The target is 'does not grow without an argument'. Two entries were deliberately NOT added while building this: the archive length rides in sm_register_lazy_file rather than an sm_declare_archive of its own, because a member is useless without it; and the container is emitted by sm_export_image_read rather than an sm_save_image, because a bare body is not an image."
    },
    "forkModuleHostImports": {
      "measure": "fork-module imports a host must satisfy (functions + reference-typed tables, excluding PIC linking); see EXPECTED_FORK_MODULE_HOST_IMPORT_COUNT",
      "ceiling": 6,
      "slack": 0,
      "target": 6,
      "why": "V4, the primary goal, and the half of it that had NO gate. EXPECTED_HOST_IMPORT_COUNT pins the KERNEL's obligation and has never let an import through unnoticed; the fork-module is a separate artifact with its own imports that every host must also satisfy, and nothing asserted them. An import was added to it on 2026-09-12 (env.__wpk_fork_host_ref_identity, maintainer-approved) and no ceiling moved -- precisely the shape this file's preamble says the ratchet exists to prevent. THE GOAL IS NOT A SMALL NUMBER FOR ITS OWN SAKE: it is that host responsibilities stay few, CLEARLY DEFINED, and THIN to implement, with the shared Wasm module doing everything else. This surface measures the 'few and clearly defined' half; the 'thin' half is measured by the TypeScript line surfaces above. SCOPE: the built artifact declares 10 import entries. Five are position-independent-code linking boilerplate any --pie side module has (env.memory, __indirect_function_table, __stack_pointer, __memory_base, __table_base), supplied mechanically by a host that knows nothing about fork; counting them would overstate the obligation and make the number move for non-fork reasons. They are pinned separately by EXPECTED_FORK_MODULE_PIC_IMPORT_COUNT. The five counted here are each a Wasm CAPABILITY FLOOR, not a design choice: resolve_externref (Wasm cannot manufacture an externref), __wpk_fork_host_ref_identity (ref.eq validates only on eqref, there is no ref.hash, and no cast rescues a host reference into the eq hierarchy), and the three reference-typed catalog/drive tables (Rust cannot declare or hold one). TARGET EQUALS CEILING because no entry has been shown reducible; if one ever is, both fall. The host-native test also asserts the NAMES, since a swap that keeps the count equal still changes what every host implements, and it compares every fork-module artifact on disk -- build-wasm.sh stages into local-binaries/ and host/wasm/ but the resolver reads local-binaries/source-only-v1/ FIRST, so a disagreement means a host would load something other than what was just built (master-plan H-9). That disagreement was real on this gate's first run.\n\nRAISED 5 -> 6 on 2026-09-13 for `env.__wpk_fork_host_func_identity`, MAINTAINER-APPROVED and conditional on the native host being able to supply it -- a condition answered with a test, not an argument (`a_native_host_can_identify_funcrefs`: `Func::to_raw` is stable per function, and a wasmtime import can take a `funcref`). \n\nWhat it buys: the module now serves `__wpk_fork_ref_encode_funcref` (unserved 6 -> 5) and it is the missing piece for `table_mutation_commit`. Wasm cannot do this for itself -- `ref.eq` validates only on `eqref` and the reference hierarchies are disjoint, measured in census section 58 -- so this is a genuine capability, not glue that could have stayed in the module. The HOST answers only 'are these the same function?'; the scan over the merged catalog, the slot-to-activation resolution, the ordinal, and the refusal of an uncatalogued function are all the module's. \n\nTarget moves with the ceiling: 6 is the floor to hold, not a gap to close."
    },
    "parseShebangReferences": {
      "measure": "references to parseShebang in host/src",
      "ceiling": 12,
      "slack": 12,
      "target": 0,
      "why": "Shebang is a kernel feature on every real Unix: execve(2) substitutes the interpreter. POSIX does not specify it at all and only requires libc's execvp family to fall back to sh on ENOEXEC, so a libc-only implementation is a divergence rather than a simplification. The lane is done when the host no longer parses it."
    },
    "setuidLazyWithoutDigest": {
      "measure": "packages with mode 4755 that ship lazy while the rootfs manifest emitter writes no digest for lazy outputs",
      "ceiling": 2,
      "slack": 2,
      "target": 0,
      "why": "sudo and sudo-lite ship setuid-root as lazy references fetched by URL with length as the only check. Bytes of the same length from a substituting host, a poisoned cache or a network position execute as root inside the guest."
    },
    "kernelWorkerTypeScript": {
      "measure": "code lines (non-blank, non-comment) of host/src/kernel-worker.ts",
      "ceiling": 26495,
      "slack": 400,
      "target": 9700,
      "why": "The single largest file in the host and the one every lane contends for. **Target validated by the K1 census, no longer provisional**: crates/host-native's guest.rs does the same job \u2014 fork, exec, clone, the blocking-retry pump \u2014 in 13,577 lines, against CentralizedKernelWorker's 29,975. The 12,000 target was guessed as 'roughly a third' and the native host independently lands near it. Note the measurement hazard: moving code inside this file shifts V8 parse and compile for unrelated functions, which was visible in the select A/B. Re-baselined 2026-09-12 from total lines to CODE lines: ceiling 32718 -> 26495, target 12000 -> 9700. Same code, a measure that counts implementation."
    },
    "unguardedLayoutModules": {
      "measure": "process_layout modules whose C-side _Static_asserts are not anchored to a generated KANDELO_ macro",
      "ceiling": 0,
      "slack": 0,
      "target": 0,
      "why": "Lane G. **Measure corrected twice while implementing G1, and the second correction retracts a claim in the first.** (1) It originally counted modules the generated header DELIVERS as #defines, which extending the emitter would have driven to 0 while guarding nothing (H-2). (2) The replacement said 'no struct-layout assert exists anywhere in libc' \u2014 WRONG: libc/musl-overlay/arch/wasm32posix/bits/stat.h has 8, missed because they use __builtin_offsetof. The real defect is sharper: those 8 asserts compare C against **hand-written literals** (112, 32, 40...), not against the Rust authority, so 0 of 15 modules are anchored to the generated constants. The overlay header states in prose 'The complete layout MUST match crates/shared/src/process_layout.rs' and nothing enforces it \u2014 two sources agreeing by convention, the campaign's recurring shape. statx is the proven instance: stx_dev_minor was never written."
    },
    "kernelHostImportTypeScript": {
      "measure": "code lines (non-blank, non-comment) of host/src/kernel.ts",
      "ceiling": 3822,
      "slack": 150,
      "target": 2100,
      "why": "The implementation behind lane I's 72 host imports; its host_* definitions span 3,771 of 4,775 lines. **Target re-derived by the I/X surface census (docs/plans/2026-09-11-lane-i-x-surface-census.md), which refuted the guessed 1,200**: host-native supplies the same surface through 71 linker.func_wrap calls spanning 7,646 lines of guest.rs, so there is no evidence the implementations shrink to 1,200. Lane I's real lever is the NUMBER of imports, gated separately by hostImportFunctions (72 -> 40). This target is arithmetic on that reduction \u2014 4,774 x (40/72) \u2014 a consequence of the lane succeeding rather than an independent goal, and it is not a measurement. Re-baselined 2026-09-12 from total lines to CODE lines: ceiling 4774 -> 3822, target 2650 -> 2100. Same code, a measure that counts implementation."
    },
    "hostKernelPlumbingTypeScript": {
      "measure": "code lines (non-blank, non-comment) of host/src/kernel-scratch.ts, kernel-entry-gate.ts, process-memory.ts and worker-protocol.ts",
      "ceiling": 4734,
      "slack": 150,
      "target": 2900,
      "why": "Lane L. What a wasmtime host builds around the imports: entry serialization, capacity-carrying scratch views, process memory layout and the worker wire. **Target set by the L1 census (docs/plans/2026-09-11-lane-l1-census.md), not provisional.** The census raised it from a guessed 1,500 to a derived 3,600: ~428 lines are JavaScript self-defence a native host never writes, worker-protocol.ts (429) is JS-host structural because host-native uses native threads, and the re-entrancy gate stays because Rust gets it from the borrow checker and JavaScript cannot. What actually moves is duplicated knowledge \u2014 one process-memory layout, one bounds-check rule, and a generated scratch pointer table. Re-baselined 2026-09-12 from total lines to CODE lines: ceiling 5853 -> 4734, target 3600 -> 2900. Same code, a measure that counts implementation."
    },
    "imageBuilderFilesystemImporters": {
      "measure": "files under images/ importing host/src/vfs/memory-fs or sharedfs-vendor",
      "ceiling": 36,
      "slack": 0,
      "target": 0,
      "why": "Lane Y, set by the Y1 census (docs/plans/2026-09-11-lane-y1-census.md). It replaces a line-count surface that measured the wrong thing: the builders' 13,502 lines are overwhelmingly recipes \u2014 which packages go in the LAMP image, how WordPress is preinstalled \u2014 and that product configuration stays. A line target invited deleting exactly the wrong lines. This counts the coupling instead, which is the fact lane V is blocked on: memory-fs.ts cannot be deleted while 36 builder files import it. Goes to 0 exactly when the lane is done."
    },
    "hostPeerDuplicateDeclarations": {
      "measure": "declaration names appearing in both halves of the three browser-/node- pairs",
      "ceiling": 29,
      "slack": 0,
      "target": 29,
      "why": "Lane E. Counts declaration names present in both halves of a browser-/node- pair, excluding members destructured from the shared createProcessLifecycle factory. **Target corrected from a guessed 12 to a derived 29 by the E3 audit (2026-09-12).** The 21 same-named worker-entry declarations were read one at a time: 4 are `let` MODULE STATE initialised from shared constants (maxPages, initReady, defaultThreadSlots, processMemoryAllocator) which cannot be shared because each host needs its own instance, and 17 differ for platform reasons \u2014 vforkMechanismTraceEnabled reads process.env in Node and a runtime message in the browser, which has no env vars. None is duplication to remove. The floor is 21 worker-entry + 6 genuinely-different protocol types + 2 message unions = 29."
    },
    "artifactTierPathSpellings": {
      "measure": "independent production spellings of the local-binaries/source-only-v1 tier path",
      "ceiling": 1,
      "slack": 0,
      "target": 1,
      "why": "Lane R, set by the R1 census. Replaces a line-count surface that would be satisfied by deleting policy that is not duplicated and untouched by fixing the duplication that is. host-native's equivalent of the 4,020-line TypeScript resolver is ten lines; what duplicates is the tier list and its order, spelled 8 times across the writer (xtask local-build, 7) and both readers. The drift already cost 39 of 53 host-native tests against a tree where the build had just succeeded."
    },
    "sessionHandMaintainedSyscallNames": {
      "measure": "entries in SYSCALL_NAMES_LOCAL in web-libs/kandelo-session/src/kernel-host.ts",
      "ceiling": 0,
      "slack": 0,
      "target": 0,
      "why": "Lane W, set by the W1 census. A hand-maintained syscall-name table sitting beside a generated one: ABI_SYSCALL_NAMES has 233 entries, this copy has 137. 96 syscalls render in the UI as syscall_NNN, and 2 names actively disagree (129 statfs vs statfs64, 130 fstatfs vs fstatfs64). The comment justifying the copy cites avoiding a heavyweight kernel-worker import, but the generated table lives in host/src/generated/abi.ts, a leaf module with no Node-only imports."
    },
    "sessionKernelFormatParsers": {
      "measure": "hand-written parsers of kernel-emitted text formats in web-libs/kandelo-session",
      "ceiling": 5,
      "slack": 0,
      "target": 0,
      "why": "Lane W. parseMaps, parseMounts, parseProcEntry, parseStatusBytes and parseRangeSize parse /proc/<pid>/maps, /proc/mounts and /proc/<pid>/status with hand-written regexes. The kernel holds this data structured, serialises it to text, and the UI re-derives it with nothing binding the two. Lane G's failure mode in the UI layer."
    },
    "buildFreshnessDigestsOutsideRust": {
      "measure": "non-test scripts under scripts/ computing a build-freshness digest",
      "ceiling": 14,
      "slack": 0,
      "target": 6,
      "why": "Lane U, set by the U1 census. Replaces two line-count surfaces that measured size \u2014 the axis the lane's own text says is wrong \u2014 and would have been satisfied by rewriting loud, harmless test runners. This counts the tier that can silently produce a wrong artifact. The archetype is build-step-input-hash.sh: a content-identity cache-key primitive written in shell, consumed by build-host.sh, build-rootfs.sh AND tools/xtask/src/local_build.rs, and mirroring a second shell script that folds the same logic. The repo has already served a stale kernel from a cache key that omitted an input. Target is 6, not 0: release verification and CI deployment checks legitimately compute digests in shell and fail loudly."
    },
    "kernelWorkerClassMethods": {
      "measure": "methods declared in class CentralizedKernelWorker",
      "ceiling": 522,
      "slack": 10,
      "target": 150,
      "why": "Lane K, added by the K1 census. 91.6% of kernel-worker.ts is one class: 522 methods over 29,780 lines. A line-count gate alone permits shuffling code between methods of the same god class while nothing improves; method count in the largest class does not. The lane is decomposition, not migration \u2014 the 85 syscalls the host dispatches genuinely need host services, and host-native dispatches them too."
    },
    "forkGuestImportsUnserved": {
      "measure": "canonical fork imports in the crates/shared ProgramArtifactImport table that crates/fork-module does not export under the same name",
      "ceiling": 3,
      "slack": 0,
      "target": 0,
      "why": "The guest declares 46 fork imports. Each one the fork-module exports VERBATIM can be wired with a nameless loop -- host-native already does this for the five frame imports. Each one exported under a different spelling (fm_ref_gc_route for __wpk_fork_ref_gc_route) forces a mapping table back into the host, which is the hand-maintained-knowledge-beside-a-generator defect three censuses in this campaign have found. Target 0 is the point at which the JS host layer is pure wiring. The 19 -> 18 step, banked 2026-09-12, is __wpk_fork_ref_exn_define: the one define in the family that needs nothing but guest linear memory, because fork-instrument's exception codec stages scalars AND the payload recipe ids into a single scratch span before the call. GROUNDED BREAKDOWN of the remaining 18, read at each emission site in crates/fork-instrument rather than inferred from signatures: 5 are the module_state_table_* mutation group, blocked on an undefined journal format (maintainer decision). Of the other 13: TWO need no new fm_* entry at all (gc_define and gc_provenance_end are pure scalars over guest memory) but both are coupled to the provenance registry and must land with it -- serving gc_define alone would bake in 'provenance is always absent', which is only true because nothing serves provenance_begin yet. TEN each need an injected wasm shim backed by a new fm_* helper: seven because the value arrives in a reference-typed TABLE slot (gc_capture_layout, gc_broker_encode, gc_provenance_begin, gc_provenance_ref, exn_lookup, exn_claim, exn_broker_encode), two because they must THROW so the guest's try_table catches the exnref -- the emitter puts `unreachable` after the call, so returning normally is a bug (exn_ingress_throw, exn_broker_throw_recipe) -- and one because it is externref in and externref out (provenance_externref). The LAST one, encode_funcref, is the only remaining import that grows the HOST contract: it takes the funcref as a parameter and must return its recipe id, wasm cannot compare funcrefs, and funcref is not a subtype of anyref, so the approved __wpk_fork_host_ref_identity cannot serve it -- it needs a funcref-identity host import of its own. So the tranche is about +11 fm_ entries against a ceiling of 54, plus one new host import. That collision is what has to be resolved before it can land. The 18 -> 15 step, banked 2026-09-12, is the constructor-provenance trio (__wpk_fork_ref_gc_provenance_begin/_ref/_end). begin and end are pure scalars and needed no shim at all; only _ref does, because it moves the guest's staged seed from the transit table into a module-owned WITNESS table. Crucially it needs no host import and no reference identity: the witness is keyed by (layout, ordinal), both plain integers the guest already passes, which is what keeps it off the allocation hot path. See docs/plans/2026-09-12-lane-f-census.md sections 21 and 22. The 15 -> 13 step, banked 2026-09-12, is __wpk_fork_ref_exn_lookup and __wpk_fork_ref_exn_claim, both pure Rust with no shim and no host surface. Lookup always reports NOT FOUND, so every catch takes a fresh recipe. That is a measured decision, not a shortcut: NOTHING can dedup an exception. Wasm cannot -- `exn` is a disjoint hierarchy, so ref.eq on two exnrefs does not validate, an exnref cannot enter an anyref table, and no cast rescues one into the eq hierarchy; only ref.is_null accepts an exnref at all. And a JS host cannot -- an exnref VALUE cannot cross into a JS import, which compiles and instantiates then throws TypeError at the first call. The same limitation is why it costs nothing: a guest cannot observe that two exnrefs are distinct either, and payloads still dedup through the normal identity path, so two exception recipes share their payload objects. Measured with wasm-tools validate against a positive AND a negative control; see census sections 28a, 28b and 29. The 13 -> 12 step is __wpk_fork_ref_exn_broker_encode, the unknown-tag path, served as a LOUD REFUSAL: EOPNOTSUPP and a poisoned recipe. A foreign exception is opaque on every axis -- its payload needs catch_ref against a tag this module does not have, it cannot be identified, and it cannot be handed to a host to inspect. Real handling means routing to the activation whose codec owns the tag, which needs a capture-side drive that does not exist (F3). The refusal is structural rather than a gated placeholder BECAUSE no gate signal is exported -- fm_stats has no gated counter -- so a placeholder would let the child rebuild the i31(0) sentinel where an exception had been and say nothing. The poisoned value is not a valid recipe id, so any edge naming it is rejected at define and the capture cannot seal. That restriction (no fork while a foreign exception is live) is stated, not hidden, and is not a regression: an unserved import leaves the same case to a host that cannot inspect the exception either. Census section 30. The 12 -> 11 step is __wpk_fork_ref_gc_define, and it is the first time the module DRIVES THE GUEST to capture rather than to replay. Constructor-provenance witnesses are interned lazily inside it, through an injected shim that stages the witness in the anyref transit slot and call_indirects the guest's own __wpk_fork_ref_gc_encode_slot at drive slot 11 -- so the module gets a recipe for a reference Rust can neither hold nor describe. The recipe is CACHED per witness, since a witness is shared by every object of its layout. \u00a721 established gc_define must not be served without this: alone it would pass no provenance for every object and bake in 'provenance is always absent'. No host import was added and no target-0 ceiling moved -- the shim is reached through the placeholder-import pattern, so the emitted module carries no unresolved import. Census sections 38, 39, 40 and 41. The 11 -> 10 step is __wpk_fork_ref_gc_capture_layout, answered by driving the guest's own TYPE-TEST probe at drive slot 12 rather than by keeping a map. A layout is a per-OBJECT fact -- two objects of one base type can be made by different constructors -- so the witness trick that made provenance bounded does not apply, and recording it per object is the unbounded storage problem of census \u00a720. Asking the guest costs nothing and stores nothing: the value is already in the transit slot and the guest's generated codec ref.tests it, returning (type_ordinal << 32) | layout_id. The caller's static layout argument is deliberately NOT trusted over the type test. Census \u00a742. The 10 -> 9 step is __wpk_fork_ref_gc_broker_encode, the cross-activation path: a structurally canonical GC value can enter through another dynamically loaded module, whose codec is the one that can encode it. The module cannot inspect a reference, so it ASKS each registered activation's codec through the drive table and routes to the first CLAIMANT -- probe then encode, both the guest's own generated functions. Bounded by the number of registered activations, not by objects. An unclaimed value is EOPNOTSUPP and a poisoned recipe rather than an invented one, the same structural refusal the exception broker uses. Census \u00a743.\n\nBANKED 4 -> 3 on 2026-09-13. `table_state_owned` moved into the module. The host still ELECTS which coordinate owns a physical table -- that compares `WebAssembly.Table` object identity, which wasm cannot do -- but it now SEEDS the result through `fm_set_activation_table_state_owner` instead of answering a callback per call, and the module serves the guest import from what it was told. Electing and answering were two jobs in one host function; only the first needs JavaScript.\n\nWhat remains is three, and all three are arguable rather than pending: the two `exn_*` throws the maintainer deferred (a JS import cannot re-enter wasm THROWING a tagged exception) and `provenance_externref` (reads a handle off a token and keys a map by object identity). This surface's target of 0 assumes all three eventually move; that assumption is now the whole remaining distance."
    },
    "forkGuestObjectImportsUnserved": {
      "measure": "non-function guest fork imports (tables, globals, the unwind tag) the fork-module does not define and export",
      "ceiling": 3,
      "slack": 0,
      "target": 0,
      "why": "A real fork-instrumented guest imports 51 env.__wpk_fork_* entries; the canonical ProgramArtifactImport table declares the 46 that are FUNCTIONS, because that struct carries params/results and cannot describe a table, global or tag. The other five were therefore invisible to forkGuestImportsUnserved. Counted separately because serving one is different work: a table or tag is DEFINED and exported by the module rather than implemented. __wpk_fork_ref_gc_transit and __wpk_fork_unwind are already served this way."
    },
    "forkModuleInjectorHelpers": {
      "measure": "fm_* entries called ONLY by crates/fork-module-inject (an injected wasm shim), never by a host",
      "ceiling": 15,
      "slack": 12,
      "target": 15,
      "why": "Internal implementation, NOT host surface. Each exists because the work is split across a boundary neither side can cross alone: the injected wasm shim is the only thing that can HOLD a reference, and Rust is the only thing that can hold a MAP or a counter. The shim calls the host import to turn its reference into an integer, then calls into Rust with that integer. A host never sees these, so they add nothing a host must implement. Currently 3: fm_drive_bump, fm_gc_identity_find, fm_gc_identity_claim. CEILING 15 IS A PRE-AUTHORIZED ENVELOPE, not a measurement: ten of the eighteen remaining guest imports each need one of these (seven read a reference-typed table slot, two must THROW so the guest's try_table catches the exnref, one is externref in and out), so this surface is EXPECTED to rise to about 15 as they land. Slack 12 is what lets 3 pass today without the ceiling being wrong. Exceeding 15 is a decision, not a rounding. Unlike every other entry here this one is not trying to reach zero -- the floor is however many such boundaries exist."
    },
    "forkModuleEntriesWithoutProductionCaller": {
      "measure": "fm_* entries no production host and no injector shim calls -- reached only by tests, or by nothing",
      "ceiling": 46,
      "slack": 46,
      "target": 0,
      "why": "H-1 at scale: dead floor reads as complete. 27 of 54 entries are reached only by host/test, crates/fork-module/tests/*.mjs, or nothing at all. WHY THE TEST-ONLY AND NEVER-CALLED GROUPS ARE ONE NUMBER: ratcheting test-only entries separately would reward DELETING TESTS to make a ceiling pass, which is a perverse incentive a ratchet must not create. Merged, the only ways down are the two that are actually wanted -- wire an entry to a production caller, or delete the entry. Deleting its test moves it from test-only to never-called and changes nothing. NOT ALL OF THIS IS DEAD, and it must not be deleted to bank a reduction: fm_frame_{reserve,commit,peek,next}, fm_resume_peek, fm_set_activation_resume_catalog and fm_activation_module_buffer all take an explicit activation_id, where the guest-facing __wpk_fork_frame_* counterparts call the IDENTICAL *_impl with primary_activation(). They are the multi-activation (dlopen fork) variants -- a fork across N dynamically loaded libraries needs each activation's own resume catalog and frame cursor, because resume-slot numbering must match that activation's table by construction. Pending capability, not dead code. fm_abort is the one that is neither, and may be a live gap: it releases fork chunks without requiring the replay to have finished, its abort_impl is reachable from nowhere else, and its doc says it mirrors the JS backend's abort() -- which Phase 4 deleted. Whether host-native has an error route that leaks chunks is NOT established. COUNTED BY CALLER, NOT BY MENTION: the measure strips comments first. Without that, a doc comment naming an entry read as a caller -- one sentence of prose in host/src moved fm_gc_identity_find out of the injector-only bucket -- while string literals are deliberately KEPT, because host-native binds its drive surface by name inside one. Census: section 10.\n\nNO LONGER RATCHETED ALONE from 2026-09-13: `forkModuleHostEntries` ratchets this bucket together with the host-called one, because a reconnection moves entries between them without adding anything. Slack is set to the ceiling so this stays MEASURED and visible -- its target of 0 is still the truth that an entry nothing calls is a defect -- without failing the build for a movement the paired ratchet already accounts for.\n\nCEILING SET TO THE PAIR'S TOTAL (46) on 2026-09-13, which makes this surface REPORTED rather than enforced -- the shape the maintainer's merge decision actually asked for. Setting `slack` to the ceiling only freed the LOWER bound; the upper bound was still gating, so an entry moving between the two buckets still failed the build as if something had grown. It cannot exceed 46 by construction (the two buckets partition one set), so this number gates nothing and the target of 0 stays as the statement it was always meant to be: an entry nothing calls is a defect. `forkModuleHostEntries` is the ratchet."
    },
    "forkPlatformTypeScript": {
      "measure": "code lines (non-blank, non-comment) of the thin layer this lane authors, named file by file",
      "ceiling": 759,
      "slack": 450,
      "target": 500,
      "why": "The PLATFORM half of stage 2: worker spawn, vfork address-space lifetime, cross-worker replay commit ordering, the host import runtime. Currently 0 -- this lane set all of it aside in attic/fork-typescript-do-not-use/ and none has been rewritten yet. CEILING 450 IS A PRE-AUTHORIZED ENVELOPE, not a measurement, granted 2026-09-12 on the same terms as the module half's 250 was: it must be BANKED to the real figure once the half stops moving, and a ceiling left above what exists is exactly the unbanked slack this file exists to prevent. Slack 450 is what lets 0 pass today. THE ESTIMATE IS SOFT AND LIKELY HIGH: census section 23 measured what each set-aside module actually depends on and found three of five are neither coordination nor floor -- fork-process-continuation alone is 1,471 lines of DRIVER LOOP that should collapse under the F3 coarsening rather than be rewritten, and fork-replay-gate's shared-i32 gate is expressible in wasm once it moves out of its own SharedArrayBuffer. Target 300 reflects that. The genuine floor here is narrow: worker creation, message transfer, and the async lifecycle the module cannot observe.\n\nFIFTH FILE ADDED 2026-09-13: `host/src/fork-table-state-owners.ts`. NO CEILING CHANGE -- the authored layer measures 275 against a ceiling of 450. It is listed here rather than left to the `fork-*.ts` glob because the glob puts it in `forkRestoredHostFloor`, which is banked at its measurement and must only ever fall; a newly AUTHORED file landing there would read as restored attic code and quietly consume the one surface that cannot absorb growth. The budget caught exactly that (3919 > 3894) before this commit, which is the check working.\n\nWhat the file is: the election deciding which `(activation, owner)` coordinate writes a physical table's sparse state. It is floor because the decision is made by comparing `WebAssembly.Table` OBJECT IDENTITY -- wasm has no `table.eq`, and the fork module does not import the activations' tables at all. What it deliberately is NOT is the dirty-page journal those coordinates write into: that is the module's, and the 3825-line `fork-module-state` arena it used to share a file with stays in the attic.\n\nPROVISIONAL RAISE 450 -> 463 on 2026-09-14 for `host/src/fork-phase.ts` (28 code lines), the host half of the `fm_phase` change. This is the surface the raise belongs on: it is the thin layer this lane AUTHORS, it is the one surface in this file whose TARGET (500) is above its ceiling, and its own text above calls 450 a pre-authorized envelope rather than a measurement. 463 is still under the target. Adding the file to the measured list -- in both this surface and the `forkRestoredHostFloor` subtraction -- is the visible decision that file-by-file list exists to force.\n\nPROVISIONAL RAISE 463 -> 671 on 2026-09-14 for `host/src/fork-import-identity.ts` (208 code lines), and THIS ONE CROSSES THE TARGET OF 500, which this file's own text calls a stop-and-talk line. Reported rather than quietly spent; the maintainer's instruction with that number was to do the work provisionally and raise it with them, which is what this is.\n\nWhat the 208 lines are, because the size is the question: extracting the KFIG/KFIT custom sections (`WebAssembly.Module.customSections`, which only the host can call); numbering imports by their position in the import section (`WebAssembly.Module.imports`, likewise); a recording Proxy over each import namespace; publishing catalog-export identity groups; and classifying each observed value as a Global, a number, a bigint or a reference. Every one of those is a JavaScript capability the module does not have.\n\nThe recording Proxy is the part worth arguing about, at roughly 28 lines. It exists because one `(module, name)` pair can be imported SEVERAL times and a namespace may answer with a getter, so the Nth read is the Nth declaration and resolving the name once would publish one Global for several distinct bindings. The dylink GOT imports do exactly this -- duplicate aliases are the first case the attic's own test covers -- so it is not a hypothetical.\n\nWhat it replaces: `ForkImportedGlobalCapture` and its matching, typing, sorting and encoding, 1,229 lines of attic TypeScript, of which the module now does everything but these 208. The child half (`ForkImportedGlobalPlanner`) is not written yet and will add to this number.\n\nPROVISIONAL RAISE 671 -> 677 on 2026-09-14: six lines feeding the table-state-owner election from the same catalog walk, so the `__wpk_fork_table_N` exports are read once rather than twice. The two elections over those objects are different questions -- which coordinate PROVIDES a table to a child (the module's, needing KFIT) and which one WRITES its sparse state (the host's, lowest coordinate outright) -- and keeping them in separate files is deliberate.\n\nPROVISIONAL RAISE 677 -> 730 on 2026-09-14 for `host/src/fork-activations.ts` (53 code lines), THE SECOND CROSSING OF THE 500 TARGET and reported as such. It is the host's whole memory of a process's activations, replacing a 2,098-line registry: four fields per activation, each held for something JavaScript can do and the module cannot -- the instance (binding a guest function into the drive table is a reference-typed `Table.set`, and the module is instantiated BEFORE its guests so it cannot import their exports), the module (custom sections), the frame format's fixed prefix, and the id every seed is published under. Census section 157 has the derivation.\n\nListed here rather than left to the `fork-*.ts` glob for the reason this list exists: the glob puts a new file in `forkRestoredHostFloor`, which is banked at its measurement, so newly AUTHORED code would read as restored attic code. The budget caught exactly that (4125 > 4072) before this commit.\n\nPROVISIONAL RAISE 730 -> 752 on 2026-09-14 for `ForkActivations.bootstrap`, and this one lands WITHOUT a matching deletion, which is worth stating rather than burying. It moves `registry.bootstrapActivation` -- one of the thirteen registry methods worker-main still calls -- onto the host record, so worker-main is unchanged in size and the registry keeps its other twelve callers. The line will exist when this lane is done: bootstrap runs a guest EXPORT at instantiation, before any fork exists, so there is no control flow for the module to own and no drive slot to dispatch it through. What the twenty-two lines buy over a bare call is three named refusals and one ordering rule -- an unregistered activation, a missing export, a SECOND bootstrap (destructive: it consumes active element segments the first run already took), and marking bootstrapped only after the call returns so a guest that trapped part way can be retried. The registry had all four and nobody would re-derive them.\n\nWhat still blocks the registry's deletion after this: `markTableMutation`, `captureFuncrefTablePatch` and `applyFuncrefTablePatch`, which need a (activation, owner) -> `WebAssembly.Table` map. `ForkTableStateOwners` already builds one.\n\nPROVISIONAL RAISE 752 -> 759 on 2026-09-14 for the anyref transit view's named export checks, which are a DEFECT fix rather than a feature. Its one production call site passed the transit TABLE where the module's EXPORTS belong, so the view threw at construction saying \"the fork module exports no __wpk_fork_ref_gc_transit table\" -- true of the argument and useless about the mistake -- and every fork-instrumented process worker failed there. The seven lines read `fm_transit_grow` and `fm_last_errno` by name too and say which one is missing, so the next stale call site is attributable.",
      "rationale": "The PLATFORM half of stage 2: worker spawn, vfork address-space lifetime, cross-worker replay commit ordering, the host import runtime. Currently 0 -- this lane set all of it aside in attic/fork-typescript-do-not-use/ and none has been rewritten yet. CEILING 450 IS A PRE-AUTHORIZED ENVELOPE, not a measurement, granted 2026-09-12 on the same terms as the module half's 250 was: it must be BANKED to the real figure once the half stops moving, and a ceiling left above what exists is exactly the unbanked slack this file exists to prevent. Slack 450 is what lets 0 pass today. THE ESTIMATE IS SOFT AND LIKELY HIGH: census section 23 measured what each set-aside module actually depends on and found three of five are neither coordination nor floor -- fork-process-continuation alone is 1,471 lines of DRIVER LOOP that should collapse under the F3 coarsening rather than be rewritten, and fork-replay-gate's shared-i32 gate is expressible in wasm once it moves out of its own SharedArrayBuffer. Target 300 reflects that. The genuine floor here is narrow: worker creation, message transfer, and the async lifecycle the module cannot observe.\n\nTARGET SET TO 500 by the maintainer on 2026-09-13, replacing the 300 this file previously carried. That 300 was invented, not measured -- section 13 guessed ~450 and section 23 then showed most of the set-aside modules are not floor at all, so it was halved by judgment. The maintainer's instruction with the number: minimize use of the capacity, and if the floor genuinely needs more, do the work provisionally and RAISE IT WITH THEM rather than stopping. Read 500 as a stop-and-talk line, not an allowance to spend: the risk this target carries is that an invented budget becomes a budget that gets spent.\n\nSCOPE NARROWED on 2026-09-13 from a glob over host/src/fork-*.ts to the three files this lane actually authors. The glob was correct while every other fork file was in the attic and wrong the moment any came back: it then counted newly written code and restored host floor as ONE number, which is the population mixing the maintainer had forkModuleEntryPoints split for. Restored floor now has its own surface, forkRestoredHostFloor.\n\nFIFTH FILE ADDED 2026-09-13: `host/src/fork-table-state-owners.ts`. NO CEILING CHANGE -- the authored layer measures 275 against a ceiling of 450. It is listed here rather than left to the `fork-*.ts` glob because the glob puts it in `forkRestoredHostFloor`, which is banked at its measurement and must only ever fall; a newly AUTHORED file landing there would read as restored attic code and quietly consume the one surface that cannot absorb growth. The budget caught exactly that (3919 > 3894) before this commit, which is the check working.\n\nWhat the file is: the election deciding which `(activation, owner)` coordinate writes a physical table's sparse state. It is floor because the decision is made by comparing `WebAssembly.Table` OBJECT IDENTITY -- wasm has no `table.eq`, and the fork module does not import the activations' tables at all. What it deliberately is NOT is the dirty-page journal those coordinates write into: that is the module's, and the 3825-line `fork-module-state` arena it used to share a file with stays in the attic."
    },
    "forkRestoredHostFloor": {
      "ceiling": 4072,
      "slack": 0,
      "target": 3894,
      "measure": "code lines (non-blank, non-comment) of host/src/fork-*.ts and vfork-*.ts, EXCLUDING fork-module-*.ts and the authored thin layer",
      "rationale": "Host floor restored from the attic, BANKED at its measurement so it can only shrink. The 2026-09-12 sweep moved every host/src/fork-*.ts and vfork-*.ts aside by FILENAME. Nine of those files are not fork capture/replay logic at all -- they are process lifecycle (fork-replay-gate, vfork-lifetime), cross-worker host-import transport (fork-externref-import-mailbox, fork-host-import-runtime, fork-worker-import-exceptions, fork-worker-exception-capability), the externref broker the Rust-first contract names as floor (fork-reference-broker), and memory placement (vfork-workspace, fork-continuation). Keeping them in the attic broke 38 test files and 843 passing assertions for no gain. \n\nTHE TARGET IS NOT MINE TO SET and is deliberately parked equal to the ceiling. Some of this should still shrink -- 1165 lines of externref mailbox and 796 of worker import exceptions are large for 'floor' -- but inventing a number here is precisely what the maintainer warned about: a target I invent becomes a budget I then spend. The ratchet already works with ceiling == target: this can only go down. \n\nEvery file here is argued in docs/plans/2026-09-12-lane-f-census.md section 64, with its live consumers and why it is not capture logic. fork-module-state (3825 lines) was NOT restored for the opposite reason: it is a second implementation of the KFMS wire format the module owns, which is the decoder-drift hazard the campaign exists to remove.\n\nRE-BANKED 3777 -> 3894 on 2026-09-13 for `fork-resume-catalog` (the tenth file, a 134-line reader of the host-known resume-catalog custom section that `fm_set_resume_catalog` requires the host to locate). This number moves ONLY while the attic audit is still running, and only ever by a file being RECLASSIFIED from the attic to floor -- never by a line being written. Once the audit is done it can only fall. A rise here from authored code would be the abuse this surface exists to prevent, and is distinguishable in the diff: a reclassification is a `git mv` with no content change, which is what both of these were.\n\nRAISED 3894 -> 4072 on 2026-09-13 for `fork-externref-process-owner`, and this is the reclassification this surface's own note says it may move by -- with one difference worth stating: it is a `git mv` MINUS a method. The file is the kernel-Worker owner of opaque host references, imported by `process-lifecycle.ts` and BOTH kernel-worker entries, so production had been importing a module that was not there. Its one attic-coupled method -- which read the parked parent's KFMS arena and ran the full segmented-transaction parser to re-derive the externref handles -- is gone, so `fork-module-state` (3825 lines) and `fork-reference-wire` (1131) did NOT come back with it. 178 lines restored instead of 220, and ~4,956 lines of duplicate decoder avoided."
    },
    "forkModuleHostEntries": {
      "ceiling": 56,
      "slack": 2,
      "target": 5,
      "measure": "fm_* entries in crates/fork-module/src/lib.rs that a production host calls, PLUS those nothing calls (the two buckets a reconnection moves entries between)",
      "rationale": "ONE ratchet over two buckets, by maintainer decision 2026-09-13, replacing the separately-ratcheted `forkModuleHostDriveEntries` (25) and this surface's own ceiling.\n\nSplitting them was right while they moved independently. They stopped: reconnecting the thin layer gives an EXISTING entry a production caller, so it changes bucket and the two move equal and opposite. That happened three times in one day -- 25+21, 30+16, and once more -- total 46 every time, and each looked like a raise in isolation. Two of the day's ceiling moves would not have happened under this measure.\n\nThe sum falls only when the module ABSORBS work or an entry is deleted, which is what this surface was always about. Target 5 carries over from the old drive-entries surface: the inversion target is 3-5 coarse entries a host must call. Both halves are still measured and reported separately for visibility -- see `forkModuleEntriesWithoutProductionCaller`, whose own target of 0 still says a dead entry is a defect -- but neither is ratcheted alone.\n\nThe import obligation is a DIFFERENT and more expensive surface: see `forkModuleHostImports`.\n\nRAISED 46 -> 47 on 2026-09-13 for `fm_set_activation_table_state_owner`, with the maintainer's explicit approval on the framing that the fork module gains a function the fork-instrumented GUEST imports. That framing is accurate for the guest-facing half -- `__wpk_fork_module_state_table_state_owned` is now a module export (plus a sixth `__wpk_fork_activation_trampolines` slot for the multi-activation path) -- and this entry is the other half: the once-per-coordinate seeding call the HOST makes so the module knows the election result.\n\nSo the trade is exact: every JS host stops implementing one guest import and starts making one seeding call. `forkGuestImportsUnserved` falls 4 -> 3 in the same commit. This surface and that one pull against each other by construction -- closing a guest import costs an entry here -- and the maintainer's ruling was that guest imports win, because an import is a contract every host must SATISFY while an entry is a call it makes.\n\nRAISED 47 -> 49 on 2026-09-13 for `fm_captured_externref_count` and `fm_captured_externref`. The module is GIVEN each broker handle on `fm_capture_intern`, so recording them costs it nothing; these two let the parent read back what it interned and hand the list to the kernel worker. Two entries against ~4,956 lines of host decoder that existed only to re-derive the same set -- and the decode leaves the one thread every process's syscalls serialize through.\n\nPROVISIONAL RAISE 49 -> 50 on 2026-09-14 for `fm_phase`, and this one was asked for and approved in advance. The module owns the fork phase machine; the host kept a second copy of the phase in the JS coordinator and answered from it. Two authorities for one fact, and the host's was the one that could enforce nothing -- the module already refuses every out-of-phase entry with EBUSY. One read-only export replaces the mirror. It moves this surface AWAY from its target of 5, which is the cost being paid knowingly: the mirror it deletes is worth more than the entry it adds.\n\nPROVISIONAL RAISE 50 -> 51 on 2026-09-14 for `fm_borrowed_replay_workspace`, which replaces `ForkProcessContinuationCoordinator.borrowedReplayWorkspaceRequirements()`. The host was reaching into every activation's frame format for its fixed prefix and into the capture session for the reference-scratch high-water -- per-activation module state and the module's own allocator, read through a JS mirror of both. One field-indexed entry (the `fm_stats` shape) rather than two, and the sealed-parent rule the JS requirePhase enforced is now enforced by the module's phase machine instead of a copy of it.\n\nPROVISIONAL RAISE 51 -> 52 on 2026-09-14 for `fm_module_state_arena(op, arg)`, the module half of the KFMS arena port. ONE field-indexed entry covering four operations (root, adopt, release, owned) rather than four exports -- the shape `fm_stats` established and the shape this surface's target of 5 is asking for. It replaces the largest file in the attic (`fork-module-state.ts`, 3,825 lines) behind seven host methods (census section 132). IT HAS NO PRODUCTION CALLER YET, and that is deliberate rather than an oversight: census section 133 found that the module ALLOCATES the arena chunks while the host FREES them, having rediscovered the addresses by walking the linked chunk list in guest memory, so the two halves cannot be switched a method at a time -- if both free, a fork double-munmaps; if neither does, it leaks. The module half lands first, tested, and is called when the host's `release()` goes away in the same change. That means this raise also carries one entry into `forkModuleEntriesWithoutProductionCaller` until then.\n\nPROVISIONAL RAISE 52 -> 53 on 2026-09-14 for `fm_set_activation_template_id`, predicted by census section 139 before it was built. The 32-byte module template id is a hash of the guest module BYTES, which only the host holds, so it is seeded rather than computed -- the same family as `fm_set_activation_catalog_base` and `fm_set_activation_gc_codec`. It buys the module writing the arena's `Module` records itself, which removes the JS registry's `arena.appendModule` loop and with it the last writer into the arena that is not the module. Without those records the arena has no activation set at all: the child-install path filters on that record kind to decide which activations to drive.\n\nPROVISIONAL RAISE 53 -> 54 on 2026-09-14 for `fm_set_activation_imported_globals`. Capability limit, stated as D4b now requires: a custom section lives in the `WebAssembly.Module` and only the host can extract it (`WebAssembly.Module.customSections`) -- the module cannot reach its guests' modules at all. Same forcing reason as the GC codec seed beside it. It buys the module the imported-global declarations it needs to build the binding records itself, which is the half of `appendTo` that never needed JavaScript.\n\nPROVISIONAL RAISE 54 -> 55 on 2026-09-14 for `fm_set_imported_global_provenance`. Capability limit: wasm cannot compare object identity -- there is no `global.eq` and this module does not import the activations' globals -- so whether an import's value is a `WebAssembly.Global`, and which activation exports that same object, can only be resolved in JavaScript; and a plain scalar import's value lives in the import object, which the module cannot see. Those two facts are the entire host contribution to a binding. It buys moving the rest of `appendTo` into Rust: the type code comes from KFIG, the recipe id from the arena's snapshots, and the sorting and encoding are already there.\n\nPROVISIONAL RAISE 55 -> 56 on 2026-09-14 for `fm_set_global_identity_group`, which SPLITS the entry above rather than adding to it: `fm_set_imported_global_provenance` lost two arguments in the same commit and no longer carries a coordinate at all. Capability limit: two activations can import the same `WebAssembly.Global` object, and wasm cannot compare object identity -- there is no `global.eq`. Only JavaScript can say that two catalog entries are one object, so the host publishes identity GROUPS. What it buys is the decision that used to sit beside the comparison in JavaScript: which member of a group PROVIDES the object. `fork_instrument` exports `__wpk_fork_global_N` for every global an activation has, imported ones included, so a group is usually one owner and several importers, and only the KFIG sections seeded into this module tell them apart. Publishing a resolved coordinate would therefore have required the host to decide something it can only decide by duplicating KFIG knowledge; publishing the group requires nothing but the identity comparison. The election, the lowest-coordinate tie-break and the BASE_IMPORT fallback are now four unit tests in fork-codec rather than untested host JavaScript, and BASE_IMPORT is refused as an INPUT at the seed -- it is a conclusion the module reaches, never a claim the host may make."
    },
    "forkAtticImports": {
      "measure": "distinct modules under attic/fork-typescript-do-not-use/ that host/src still imports",
      "ceiling": 8,
      "slack": 9,
      "target": 0,
      "why": "THE ONLY SURFACE THAT MEASURES THIS LANE'S GOAL. Every other number in this file counts code that EXISTS. This one counts code the host still DEPENDS on, and it is the difference between the two that made a day of migration read as regression: the replacement being built is measured, and the 14,035 attic lines being replaced are measured by nothing, so moving work into the module and cutting coordinator calls showed the host surfaces GROWING. Net host TypeScript that day was +37 lines, and nobody would have known without being asked. Added 2026-09-14 at its measurement of 9, which is the count worker-main.ts carries: fork-activation-registry, fork-early-reference-provider, fork-exception-provider, fork-gc-codec, fork-imported-globals, fork-module-state, fork-process-continuation, fork-reference-segments, fork-table-snapshot. Counts MODULES, not call sites: cutting the last reference to a file is the event worth ratcheting, and cutting four of nine references to one changes nothing about whether it can be deleted. Target 0 is the lane finishing.\n\nBANKED 9 -> 8 on 2026-09-14: `fork-process-continuation` is gone. Its 1,471 lines were the fork DRIVER LOOP, and what finally made it removable was not porting the loop but deleting its callers one at a time -- the seal, the replays, the finishes and the phase read all became module calls, and what was left was bookkeeping for methods nobody called. The last two uses were `beginCapture`, which is now the registry's capture session plus `fm_parent_begin_capture` plus the launch-root write, and `prepareActivation`, whose continuation argument had no reader once the module drove the entry points."
    }
  },
  "lanes": {
    "F": {
      "title": "fork control flow \u2014 invert, do not port",
      "status": "open",
      "closure": [
        {
          "surface": "forkTypeScript",
          "atMost": 2500
        },
        {
          "surface": "forkModuleHostEntries",
          "atMost": 5,
          "note": "Was `forkModuleHostDriveEntries`. That surface merged into `forkModuleHostEntries` on 2026-09-13 by maintainer decision: the host-called and no-production-caller buckets move equal and opposite when a reconnection gives an existing entry a caller, so the pair's total is the number that only falls when the module absorbs work. The closure condition is unchanged in substance -- at most 5 fm_* entries a host must call, with none left dead."
        },
        {
          "surface": "workerMainTypeScript",
          "atMost": 3000
        }
      ],
      "why": "Inversion is done when the host calls a handful of coarse entries and no longer drives the sequence. Each fine-grained fm_* entry leaves a host-side seeding loop behind, so the entry count and the TypeScript line count fall together or neither has moved."
    },
    "V": {
      "title": "VFS image \u2014 one implementation of SFFS",
      "status": "open",
      "closure": [
        {
          "surface": "sffsTypeScript",
          "atMost": 0
        },
        {
          "surface": "memoryFsTypeScript",
          "atMost": 0
        }
      ],
      "why": "The block filesystem is implemented twice at near-identical size. The lane closes when the TypeScript copy is gone, not when the Rust one works."
    },
    "I": {
      "title": "host import surface \u2014 V4",
      "status": "open",
      "closure": [
        {
          "surface": "hostImportFunctions",
          "atMost": 40
        },
        {
          "surface": "kernelHostImportTypeScript",
          "atMost": 1200
        }
      ],
      "why": "28 of 72 imports are POSIX filesystem operations the kernel already implements for its own mounts. A host-backed mount needs roughly six calls. 40 is the ceiling to beat, not the goal."
    },
    "P": {
      "title": "platform honesty",
      "status": "open",
      "closure": "checklist",
      "why": "Not reducible to one count: the defect is capability and state presented that do not exist. The checklist is SELF-REPORTED and says so -- it enumerates the instances so none can be waved through in prose, but a tick is a claim, and each must be backed by a test demonstrated failing against the old behaviour first.",
      "checklist": {
        "mountSourceKindsOfferedWithoutImplementation": false,
        "inspectorReadsProcMountsNotTheBootDescriptor": false,
        "mountConfigReadonlyEitherGatesOrStopsRendering": false,
        "posixStatusContradictionsReconciled": false,
        "stRdevWrittenForDeviceNodes": false
      },
      "sectionsExempt": [
        "floor"
      ],
      "whyExempt": "A class of defect rather than a subsystem: there is no capability boundary to describe, so 'floor' has no meaning here."
    },
    "X": {
      "title": "process and exec",
      "status": "open",
      "closure": [
        {
          "surface": "parseShebangReferences",
          "atMost": 0
        }
      ],
      "why": "Closure is the kernel deciding what a spawn runs. parseShebang reaching zero is the measurable form; the probe traps today, so the panic is the blocker rather than the design. The processExecTypeScript surface added on 2026-09-11 was WITHDRAWN by the I/X surface census: it counted process-lifecycle.ts, which is the shared createProcessLifecycle implementation both worker entries call and which the E1 census credits as the consolidation that already happened. Shrinking it is not a goal. The exec-target authority has also already largely moved to the kernel (8 exports)."
    },
    "C": {
      "title": "conformance",
      "status": "open",
      "closure": "manual",
      "why": "Closure is a failing-file-set comparison with no campaign-caused regression, not a total. Totals hide compensating changes, and the merge-base cannot produce a comparable set because its runner times out every guest-executing test."
    },
    "S": {
      "title": "setuid lazy references are fetched without integrity",
      "status": "open",
      "closure": [
        {
          "surface": "setuidLazyWithoutDigest",
          "atMost": 0
        }
      ],
      "why": "Closure is that no setuid binary is fetched without its bytes being verified. Either the emitter records a digest for lazy outputs, or those packages stop shipping lazy."
    },
    "M": {
      "title": "shared-mapping cutover",
      "status": "deferred",
      "closure": "manual",
      "why": "Deferred by the maintainer out of #1350; resumes as a separate PR targeting its branch. Handoff at docs/plans/2026-09-11-lane-a-state-of-the-lane.md.",
      "sectionsExempt": [
        "endState",
        "floor",
        "increments",
        "acceptance",
        "hazards"
      ],
      "whyExempt": "Deferred out of #1350. Its characterization lives whole in docs/plans/2026-09-11-lane-a-state-of-the-lane.md, written by the agent that did the diagnosis; duplicating it here would create the second authority this file exists to remove."
    },
    "B": {
      "title": "build and provisioning truthfulness",
      "status": "open",
      "closure": "manual",
      "why": "Closure is that a build which cannot produce a usable artifact says so, and that the documented provisioning path and the documented freshness gate agree. Neither is a count."
    },
    "T": {
      "title": "test hygiene",
      "status": "open",
      "closure": "manual",
      "why": "Closure is every host-suite failure attributed to a named cause. Blocked on a clean suite read rather than on work."
    },
    "H": {
      "title": "browser",
      "status": "open",
      "closure": "manual",
      "why": "Deprioritized by the maintainer. Closure is a browser pass plus curation, both of which are judgement rather than measurement.",
      "sectionsExempt": [
        "endState",
        "floor",
        "increments",
        "acceptance",
        "hazards"
      ],
      "whyExempt": "Deprioritized by the maintainer, who asked to push implementation further before paying the cost of browser verification. Characterizing it now would plan work nobody intends to start."
    },
    "K": {
      "title": "kernel-worker.ts \u2014 one god class every lane contends for",
      "status": "open",
      "closure": [
        {
          "surface": "kernelWorkerTypeScript",
          "atMost": 12000
        },
        {
          "surface": "kernelWorkerClassMethods",
          "atMost": 150
        }
      ],
      "why": "The K1 census corrected the lane's framing: this is NOT the host's second syscall table. The 85 syscalls it dispatches are the ones needing a host service \u2014 worker creation, Atomics.wait blocking, shared memory \u2014 and host-native dispatches the same set. The defect is shape: 522 methods in one class, 29,975 lines, against guest.rs's 13,577 for the same responsibility."
    },
    "G": {
      "title": "ABI binding drift \u2014 all 15 layout modules anchored to generated constants",
      "status": "closed",
      "closure": [
        {
          "surface": "unguardedLayoutModules",
          "atMost": 0
        }
      ],
      "why": "A layout constant with no static assert on the C side can drift from what musl expects and nothing notices. statx is the proven instance; 9 of 15 modules are equally exposed."
    },
    "D": {
      "title": "dead Rust floors",
      "status": "open",
      "closure": "checklist",
      "checklist": {
        "fiveRemainingDeadFloorsCensused": false,
        "eachEitherWiredOrDeleted": false
      },
      "why": "Five dead Rust modules beyond shared_mapping_policy were found and never characterized. A count is the wrong measure because the answer per module is either 'give it a caller' or 'delete it', and both reduce the same number. SELF-REPORTED checklist; a tick is a claim.",
      "sectionsExempt": [
        "floor"
      ],
      "whyExempt": "Dead code has no capability boundary to describe; the floor question belongs to whichever lane owns the subsystem each module was written for."
    },
    "L": {
      "title": "Host-kernel plumbing \u2014 what a wasmtime host writes beyond the imports",
      "status": "open",
      "closure": [
        {
          "surface": "hostKernelPlumbingTypeScript",
          "atMost": 3600
        }
      ],
      "why": "Goal V1 more than V4, which the L1 census established: a new host's burden here is already near zero \u2014 host-native proves it \u2014 but the same knowledge exists twice and the second copy was made by transcription (47 comments in host-native cite host/src/*.ts). Two transcribed copies drift and host #3 transcribes a third."
    },
    "E": {
      "title": "Node/browser peer divergence \u2014 shared types unified to the audited floor",
      "status": "closed",
      "closure": [
        {
          "surface": "hostPeerDuplicateDeclarations",
          "atMost": 29
        }
      ],
      "why": "Goal V1. CLOSED 2026-09-12 at the floor the E3 audit derived. The consolidation was already largely done before the lane started \u2014 the worker-entry pair shares 72 lifecycle members through a createProcessLifecycle factory \u2014 and what remained was 36 duplicated protocol declarations, now unified. The floor of 29 is 21 worker-entry declarations that are legitimately per-host (4 module-state lets, 17 platform differences), 6 genuinely-different protocol types, and 2 message unions. No category-three divergence was ever found: no bug one host fixed and the other did not."
    },
    "R": {
      "title": "Binary and artifact resolution \u2014 one declaration of the tier order",
      "status": "closed",
      "closure": [
        {
          "surface": "artifactTierPathSpellings",
          "atMost": 1
        }
      ],
      "why": "Goal V1. Not a resolver migration: host-native reimplemented ten lines, not 4,020. The tier list and its order are spelled 8 times across the writer and both readers, and that drift has already produced a measured failure. The right fix was already applied once inside TypeScript (binary-tiers.ts) and stopped at the language boundary."
    },
    "Y": {
      "title": "VFS image builders write the image format in TypeScript",
      "status": "open",
      "closure": [
        {
          "surface": "imageBuilderFilesystemImporters",
          "atMost": 0
        }
      ],
      "why": "Goal V3, and a hard prerequisite for lane V. The Y1 census found the Rust side already covers nearly the whole builder contract across sffs_write.rs, sffs.rs and sffs_deferred.rs; the gaps are six image-level operations and three policy assertions, not a filesystem. Not a line-reduction lane \u2014 a decoupling lane."
    },
    "W": {
      "title": "web-libs session contracts a new host consumes",
      "status": "open",
      "closure": [
        {
          "surface": "sessionHandMaintainedSyscallNames",
          "atMost": 0
        },
        {
          "surface": "sessionKernelFormatParsers",
          "atMost": 0
        }
      ],
      "why": "Goals V1 and V2. The W1 census found three populations, not two: the host contract, the browser product surface that stays, and a third the lane missed \u2014 hand-written parsers of kernel-emitted /proc formats plus a hand-maintained syscall table that is 96 entries short of the generated one and disagrees with it on two names."
    },
    "U": {
      "title": "Build automation in shell and MJS",
      "status": "open",
      "closure": [
        {
          "surface": "buildFreshnessDigestsOutsideRust",
          "atMost": 6
        }
      ],
      "why": "Goal V2 and the repo's Rust-tools preference. Still does NOT serve V4 and stays ranked last. But the U1 census separates a cheap tier-1 subset (one Rust digest primitive replacing two shell copies, plus the manifest emitter lane S already needs) with already-realised failure modes, from the tier-2 bulk the census recommends not migrating."
    }
  }
}
