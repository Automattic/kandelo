{
  "$comment": [
    "Ratcheted ceilings on the surfaces this campaign exists to shrink.",
    "",
    "WHY THIS FILE EXISTS. The campaign produced 132 plan documents and one",
    "mechanism that never failed: EXPECTED_HOST_IMPORT_COUNT, a number in the",
    "repo asserted against a measurement of the built artifact. Every import",
    "change hit it. Meanwhile fork TypeScript grew by 2,867 lines over the same",
    "period with nothing to catch it, and nobody noticed for weeks. Documents",
    "are advisory; a number a test asserts is not.",
    "",
    "HOW THE RATCHET WORKS. Exceeding a ceiling fails: the surface grew.",
    "Falling more than `slack` below it ALSO fails, asking you to lower the",
    "ceiling in the same commit. That is what banks a reduction permanently --",
    "without it, one lane's deletion silently funds the next lane's growth.",
    "",
    "WHAT A CEILING IS NOT. It is not a goal. Every `target` below is far under",
    "its ceiling, and the gap is the work. See docs/plans/2026-09-11-MASTER-PLAN.md.",
    "",
    "LANE CLOSURE. `lanes` below binds each lane's status to measurements. A lane",
    "marked `closed` whose conditions are unmet fails; a lane marked `open` whose",
    "conditions are all met ALSO fails, saying to close it. Lane status therefore",
    "cannot be asserted in prose -- which is what every previous attempt did, and",
    "why drift was only ever caught when a human happened to ask.",
    "",
    "A lane whose completion is not a number says so with `manual` and a reason.",
    "That is honest; silence is not.",
    "",
    "CHECKLIST CLOSURE is weaker than a measurement and is labelled as such. A",
    "tick is self-reported. Its value is that the instances are enumerated, so a",
    "lane cannot be closed by forgetting one.",
    "",
    "CHARACTERIZATION is gated too. A lane must carry an End state, a floor, an",
    "Increments list, Acceptance evidence and Known hazards before it is",
    "dispatchable. A lane legitimately missing one declares `sectionsExempt` with",
    "a reason -- because the failure this file exists to end is a status asserted",
    "in prose that nothing checks, and that applies to its own sections first."
  ],
  "surfaces": {
    "forkTypeScript": {
      "measure": "line count of host/src/fork-*.ts and host/src/vfork-*.ts",
      "ceiling": 24726,
      "slack": 200,
      "target": 2500,
      "why": "The Rust-First contract says this shrinks toward the floor and does not grow. It grew 23,246 -> 26,113 across the campaign. The target assumes inversion to 3-5 coarse fm_* entries, which collapses transport rather than only orchestration; deletion alone cannot reach it."
    },
    "workerMainTypeScript": {
      "measure": "line count of host/src/worker-main.ts",
      "ceiling": 7499,
      "slack": 150,
      "target": 3000,
      "why": "Holds the fork drive loop. 6,083 -> 7,536 across the campaign; every fine-grained fm_* call leaves a host-side seeding and base-arithmetic loop behind by construction."
    },
    "sffsTypeScript": {
      "measure": "line count of host/src/vfs/sharedfs-vendor.ts",
      "ceiling": 3752,
      "slack": 100,
      "target": 0,
      "why": "The SFFS block filesystem is implemented twice: 3,752 lines here against 3,727 in sffs.rs + sffs_write.rs + sffs_deferred.rs, same superblock geometry, same allocators, same magic. Target is zero because one of the two must go and the kernel owns the format."
    },
    "memoryFsTypeScript": {
      "measure": "line count of host/src/vfs/memory-fs.ts",
      "ceiling": 8501,
      "slack": 200,
      "target": 0,
      "why": "The other half of the 12,198-line TypeScript filesystem the lane V text names and no register measured. MemoryFileSystem backs `/` on every machine and implements stat, statfs, pathconf, dirents, open flags and ST_NOSUID setuid handling in the host, against 1,724 lines of tmpfs.rs in the kernel. Target is 0 because lane V's end state says the host supplies nothing inside the image body; the V6 census is the deliverable that would prove any of it is floor, and until that census runs the 0 is an assertion of the plan's end state, not a measured floor. If the census finds real floor here, this target gets revised in the open with that evidence attached."
    },
    "hostImportFunctions": {
      "measure": "env.host_* function imports of the built kernel; see EXPECTED_HOST_IMPORT_COUNT",
      "ceiling": 72,
      "slack": 0,
      "target": 40,
      "why": "V4, the primary goal: how many functions a new host must implement. Already asserted against the built artifact in crates/host-native; restated here so one file carries every surface. A raw import-ENTRY count reads 73 because env.memory is an entry -- say which you counted."
    },
    "forkModuleEntryPoints": {
      "measure": "pub extern \"C\" fn fm_* declared in crates/fork-module/src/lib.rs",
      "ceiling": 69,
      "slack": 2,
      "target": 5,
      "why": "The inversion target is 3-5 coarse entries. 69 fine-grained ones are what force the host-side driver loops that make the TypeScript grow."
    },
    "parseShebangReferences": {
      "measure": "references to parseShebang in host/src",
      "ceiling": 12,
      "slack": 12,
      "target": 0,
      "why": "Shebang is a kernel feature on every real Unix: execve(2) substitutes the interpreter. POSIX does not specify it at all and only requires libc's execvp family to fall back to sh on ENOEXEC, so a libc-only implementation is a divergence rather than a simplification. The lane is done when the host no longer parses it."
    },
    "setuidLazyWithoutDigest": {
      "measure": "packages with mode 4755 that ship lazy while the rootfs manifest emitter writes no digest for lazy outputs",
      "ceiling": 2,
      "slack": 2,
      "target": 0,
      "why": "sudo and sudo-lite ship setuid-root as lazy references fetched by URL with length as the only check. Bytes of the same length from a substituting host, a poisoned cache or a network position execute as root inside the guest."
    },
    "kernelWorkerTypeScript": {
      "measure": "line count of host/src/kernel-worker.ts",
      "ceiling": 32718,
      "slack": 400,
      "target": 12000,
      "why": "The single largest file in the host and the one every lane contends for. 515 SYS_ references live here, so syscall knowledge the kernel owns is mirrored in the host. Its size is also a measurement hazard: moving code inside it shifts V8 parse and compile for unrelated functions, which was visible in the select A/B."
    },
    "unguardedLayoutModules": {
      "measure": "process_layout modules the generated musl header does not cover",
      "ceiling": 9,
      "slack": 9,
      "target": 0,
      "why": "render_process_layouts_header covers 6 of 15 layout modules, so most of the struct offsets the C side depends on have nothing checking them against musl. That is how statx came to write half a device number with no gate objecting."
    }
  },
  "lanes": {
    "F": {
      "title": "fork control flow \u2014 invert, do not port",
      "status": "open",
      "closure": [
        {
          "surface": "forkTypeScript",
          "atMost": 2500
        },
        {
          "surface": "forkModuleEntryPoints",
          "atMost": 5
        },
        {
          "surface": "workerMainTypeScript",
          "atMost": 3000
        }
      ],
      "why": "Inversion is done when the host calls a handful of coarse entries and no longer drives the sequence. Each fine-grained fm_* entry leaves a host-side seeding loop behind, so the entry count and the TypeScript line count fall together or neither has moved."
    },
    "V": {
      "title": "VFS image \u2014 one implementation of SFFS",
      "status": "open",
      "closure": [
        {
          "surface": "sffsTypeScript",
          "atMost": 0
        },
        {
          "surface": "memoryFsTypeScript",
          "atMost": 0
        }
      ],
      "why": "The block filesystem is implemented twice at near-identical size. The lane closes when the TypeScript copy is gone, not when the Rust one works."
    },
    "I": {
      "title": "host import surface \u2014 V4",
      "status": "open",
      "closure": [
        {
          "surface": "hostImportFunctions",
          "atMost": 40
        }
      ],
      "why": "28 of 72 imports are POSIX filesystem operations the kernel already implements for its own mounts. A host-backed mount needs roughly six calls. 40 is the ceiling to beat, not the goal."
    },
    "P": {
      "title": "platform honesty",
      "status": "open",
      "closure": "checklist",
      "why": "Not reducible to one count: the defect is capability and state presented that do not exist. The checklist is SELF-REPORTED and says so -- it enumerates the instances so none can be waved through in prose, but a tick is a claim, and each must be backed by a test demonstrated failing against the old behaviour first.",
      "checklist": {
        "mountSourceKindsOfferedWithoutImplementation": false,
        "inspectorReadsProcMountsNotTheBootDescriptor": false,
        "mountConfigReadonlyEitherGatesOrStopsRendering": false,
        "posixStatusContradictionsReconciled": false,
        "stRdevWrittenForDeviceNodes": false
      },
      "sectionsExempt": [
        "floor"
      ],
      "whyExempt": "A class of defect rather than a subsystem: there is no capability boundary to describe, so 'floor' has no meaning here."
    },
    "X": {
      "title": "process and exec",
      "status": "open",
      "closure": [
        {
          "surface": "parseShebangReferences",
          "atMost": 0
        }
      ],
      "why": "Closure is the kernel deciding what a spawn runs. parseShebang reaching zero is the measurable form; the probe traps today, so the panic is the blocker rather than the design."
    },
    "C": {
      "title": "conformance",
      "status": "open",
      "closure": "manual",
      "why": "Closure is a failing-file-set comparison with no campaign-caused regression, not a total. Totals hide compensating changes, and the merge-base cannot produce a comparable set because its runner times out every guest-executing test."
    },
    "S": {
      "title": "setuid lazy references are fetched without integrity",
      "status": "open",
      "closure": [
        {
          "surface": "setuidLazyWithoutDigest",
          "atMost": 0
        }
      ],
      "why": "Closure is that no setuid binary is fetched without its bytes being verified. Either the emitter records a digest for lazy outputs, or those packages stop shipping lazy."
    },
    "M": {
      "title": "shared-mapping cutover",
      "status": "deferred",
      "closure": "manual",
      "why": "Deferred by the maintainer out of #1350; resumes as a separate PR targeting its branch. Handoff at docs/plans/2026-09-11-lane-a-state-of-the-lane.md.",
      "sectionsExempt": [
        "endState",
        "floor",
        "increments",
        "acceptance",
        "hazards"
      ],
      "whyExempt": "Deferred out of #1350. Its characterization lives whole in docs/plans/2026-09-11-lane-a-state-of-the-lane.md, written by the agent that did the diagnosis; duplicating it here would create the second authority this file exists to remove."
    },
    "B": {
      "title": "build and provisioning truthfulness",
      "status": "open",
      "closure": "manual",
      "why": "Closure is that a build which cannot produce a usable artifact says so, and that the documented provisioning path and the documented freshness gate agree. Neither is a count."
    },
    "T": {
      "title": "test hygiene",
      "status": "open",
      "closure": "manual",
      "why": "Closure is every host-suite failure attributed to a named cause. Blocked on a clean suite read rather than on work."
    },
    "H": {
      "title": "browser",
      "status": "open",
      "closure": "manual",
      "why": "Deprioritized by the maintainer. Closure is a browser pass plus curation, both of which are judgement rather than measurement.",
      "sectionsExempt": [
        "endState",
        "floor",
        "increments",
        "acceptance",
        "hazards"
      ],
      "whyExempt": "Deprioritized by the maintainer, who asked to push implementation further before paying the cost of browser verification. Characterizing it now would plan work nobody intends to start."
    },
    "K": {
      "title": "kernel-worker.ts \u2014 the host's second syscall table",
      "status": "open",
      "closure": [
        {
          "surface": "kernelWorkerTypeScript",
          "atMost": 12000
        }
      ],
      "why": "The file is 32,718 lines and holds 515 SYS_ references. Syscall dispatch knowledge belongs to the kernel; a host that mirrors it is a second authority that drifts, and every lane queues behind this file."
    },
    "G": {
      "title": "ABI binding drift \u2014 generated headers cover under half the layouts",
      "status": "open",
      "closure": [
        {
          "surface": "unguardedLayoutModules",
          "atMost": 0
        }
      ],
      "why": "A layout constant with no static assert on the C side can drift from what musl expects and nothing notices. statx is the proven instance; 9 of 15 modules are equally exposed."
    },
    "D": {
      "title": "dead Rust floors",
      "status": "open",
      "closure": "checklist",
      "checklist": {
        "fiveRemainingDeadFloorsCensused": false,
        "eachEitherWiredOrDeleted": false
      },
      "why": "Five dead Rust modules beyond shared_mapping_policy were found and never characterized. A count is the wrong measure because the answer per module is either 'give it a caller' or 'delete it', and both reduce the same number. SELF-REPORTED checklist; a tick is a claim.",
      "sectionsExempt": [
        "floor"
      ],
      "whyExempt": "Dead code has no capability boundary to describe; the floor question belongs to whichever lane owns the subsystem each module was written for."
    }
  }
}
